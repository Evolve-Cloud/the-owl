# ADR-000 — Template

> Copy this file to `ADR-{NNN}-{titulo-kebab-case}.md`, increment `{NNN}` to the next sequential number, and fill every section. Delete this quote block.

**Status:** Proposed | Accepted | **Rejected** | Deprecated | Superseded by ADR-XXX

> `Rejected` cobre o ADR que foi escrito, teve a mudança aplicada e **reprovou no gate L4** — nada commitado. O ADR permanece como registro da decisão de NÃO fazer e da razão; o número nunca é reusado. Adicionado 2026-08-12 (ADR-036 foi o primeiro caso e usava um status fora do enum, que é a mesma classe de defeito — campo que parece schema e não é — um nível acima).
**Date:** YYYY-MM-DD
**Author:** @architect (or the agent proposing the decision)
**Tags:** []

## Contexto
O problema que motivou a decisão e as constraints existentes. Ground em arquivos reais (`arquivo:linha`) quando possível. Sem contexto, a decisão não é revisável.

## Decisão
O que foi decidido, em linguagem direta e afirmativa. Uma decisão por ADR.

## Alternativas consideradas
Pelo menos 2 opções, cada uma com prós e contras honestos, incluindo a opção escolhida.

- **Alternativa A (escolhida):** … — prós / contras
- **Alternativa B:** … — prós / contras

## Consequências
Benefícios esperados **e** trade-offs aceitos. O que fica mais fácil, o que fica mais difícil, que novos riscos surgem.

## Notas de implementação
Orientações concretas para o @builder executar (arquivos a tocar, ordem, testes/verificação, o que NÃO fazer).
