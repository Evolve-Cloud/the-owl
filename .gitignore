# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (if any tooling is added later)
node_modules/

# Secrets — never commit
.env
.env.*
*.pem
*.key

# the-owl loop runtime state (ephemeral; not the vault, not the config)
.owl/state/
.owl/last-run.json

# DevFlow ephemeral sessions
.devflow/sessions/

# Obsidian local UI state (keep shareable config: app/appearance/core-plugins/graph)
research-vault/.obsidian/workspace.json
research-vault/.obsidian/workspace-mobile.json
research-vault/.obsidian/cache
