# Changelog

All notable changes to the-owl are recorded here. Format loosely follows Keep a Changelog; the-owl versions its own evolution and, once the loop is live, each autonomous change lands as its own ADR + commit.

## [Unreleased]

### Session 2026-08-13 — ADR-040: capability over governance (human-directed, análise crítica externa)
- **Added — ADR-041 `sync-persona-pair.sh` (Passo 3):** o par ADR-028 vira artefato gerado — cópia-comando derivada da subagent (canônica), `--check` para o gate (substitui hash manual), `--sync` regenera. O check inaugural achou **drift pré-existente real** no par `curator` que a disciplina manual nunca pegou; corrigido, 13/13 em sincronia.
- **Added — ADR-042 (Passo 3): `inert-command-frontmatter` promovido 80→accepted, owner-directed.** `description:` aterrada em 11/11 comandos (9 `quick/*` + 2 `owl/*`) — o campo que governa o carregamento estava ausente de todos; + **`disable-model-invocation: true` em `owl/evolve.md`** (fecha o `open_for_owner` do ciclo 10: o modelo não auto-invoca mais o loop; schedule inalterado); pack `owl-agents/` re-rodado no mesmo commit (pré-condição do consumidor). Rollout de `tools:` para os 8 agentes restantes deliberadamente adiado com a razão real registrada (8 julgamentos sob fadiga = como mudança ruim entra).
- **Added — 5 capability fixtures no harness (`eval/tasks/11-15`, Passo 2):** terceira classe de fixture — mede **atualidade de conhecimento de domínio** via dimensão dominante `Domain accuracy & currency` (30–35 pts), com traps deliberados por domínio (CIDR overlap/PrivateLink, OIDC vs chaves, `CONCURRENTLY` vs lock, "initialize+Sampling" como canário da spec MCP velha, "tags imutáveis" como threat-model invertido). São a medida da matriz: aceite `classe: capability` confirma-se re-rodando o fixture antes/depois do refresh (Δ na dimensão-alvo, ADR-014/015). Staleness própria: os fatos do fixture expiram — `verified_on` 30d herdado. Baseline pendente (decisão de gasto do dono, ~2M tokens/pass).
- **Achado (Claude + codex, convergente):** o loop otimizou a governança de si mesmo — 8+ aceites de governança (ADR-016→039) vs ~2 convenções com efeito nos agentes (só `handoff-contract` com Δ provado, +11.0; `role-ownership` Δ≈0) — e o eixo estrutural de pesquisa saturou (ciclos 6–8: 0 aceites, 100% alias). O loop está saudável; a pergunta esgotou.
- **Added — ADR-040 + `research-vault/capabilities/agent-capability-matrix.md`:** eixos de **CAPACIDADE** (`platform-engineering` · `data-engineering` · `mcp-and-claude-harness` · `secure-sdlc`) viram o **default** do `{{QUERY_AXIS}}` (estruturais só por reflexão/dono); matriz operacional `agente × capacidade × fontes × eval × Δ` semeada com o estado real; **passo 2.7 do curator**: todo aceite leva `classe: capability | governance` — governance com haircut −5 + justificativa, capability nomeando célula da matriz + eval fixture (`ausente` ⇒ provisório ADR-015).
- **Superfícies (8):** matriz (nova) · `capabilities/index.md` · artefato 8a · `owl/research.md` · par `scout` · par `curator` — pares editados identicamente nas duas cópias (ADR-028). De carona, 2 afirmações estruturais expiradas ("markdown-only, no-runtime") corrigidas dentro do curator, sincronizadas com `structural-properties.md`; pesos da rubrica e `safety_floor` intocados. **0 contato com o carve-out.**
- **Follow-ups nomeados:** fixtures de eval de domínio (Passo 2 do plano); veredito capability×governance no challenger (carve-out, owner-only); higiene de grounding (Passo 0: wiki/`project.yaml`/`.claude_project` ainda com contagens e claims falsos); adicionar o par `curator` à lista "O que depende deste registro" de `structural-properties.md`.

### Cycle 2026-08-12 (cycle 9) — autonomous `/owl:evolve`: 2 landed, 1 failed the gate (shadow PR)
- **Streak broken.** Three consecutive 0-accept scored cycles (6, 7, 8) ended: 3 accepted at L2, **2 landed**, 1 caught and reverted by the gate. Axis `orchestration topology`, cutoff 2026-07-13, model `gpt-5.5`. Brief: 3 primary sources / 2 ideas, **0 aliases** of the 58 injected decided ids.
- **Added — ADR-035 (91): the L0 prompt's CONTEXT block now states the boundary, not a false capability.** Artifact 8a claimed *"markdown-only, NO-RUNTIME library of ~8 agents […] no orchestration engine, no daemon"* — **P1/P2/P3/P6 all false**, and there are **13** agents. Worse, the 2026-08-07 sweep had fixed the sibling `REJECTED CLASSES` block **of the same prompt**, so the assembled prompt contradicted itself 84 lines apart. Root cause was **not** the ADR-033 mechanism (its trigger fired and worked) but its **target list**: `structural-properties.md` did not list artifact 8a. Both fixed. The ADR's own written verification then found a **third** file (`research-brief-schema.md:44`) the plan had missed — recorded, because "the plan was incomplete and verification caught it" is information about the method.
- **Security — ADR-037 (80): `chronicler` loses network egress.** `tools: Read, Write, Edit, Grep, Glob, Bash, Skill, Agent` — derived exhaustively from the agent's own file, with the tool↔source-line table published so a reviewer checks rather than trusts. Drops `WebFetch`/`WebSearch`/MCP: its charter is *"generated FROM the code"* and it reads the whole repo, so read-everything + talk-outward is the pair worth breaking. **Slice cut to one agent, not eight** — no declared tool lists exist anywhere, so each list is a judgement and a too-short one degrades **silently**. Unblocks the id deferred since 2026-07-23; the cycle-7 question left *"for whoever re-opens that id"* is resolved: `allowed-tools` on a command is a turn-scoped **grant that widens** (*"It does not restrict which tools are available"*), so **ADR-034's asymmetry condition is satisfied and verified** — and ADR-034's falsifiable alternative B was tested and failed.
- **⛔ Rejected at the gate — ADR-036 (84): routing eligibility.** Applied `disable-model-invocation: true` to 10 command personas, **failed @guardian, reverted, nothing committed.** The ADR asserted *"no other known consumer"* after checking **one**; `grep -rl 'skill="agents:' .claude/agents/` returns **9 of 13**, and the primary doc says the field *"blocks programmatic invocation"* — it would have broken peer delegation in 9 agents, silently. Recorded lesson: verifying one case and writing "no others" is not verification, it is its absence with the wording of one.
- **🔑 Two structural properties found false this cycle.** P1/P2/P3/P6 (fixed above, found by the **ADR-033 trigger firing autonomously — its first autonomous exercise**) and **P7** — the register asserts *"hub-and-spoke, specialists never call each other — TRUE"* with `.claude/commands/agents/*.md` as proof path, and the proof path contradicts it in 9 files. P7 is classed as a **choice**, so it is **reported to the owner, not fixed by the loop**.
- **Deferred/rejected, each with a reason:** `inert-command-frontmatter` (79 — **11 of 11** command frontmatter blocks use only keys the harness ignores, and `description` is absent from all 11; deferred by **cap + adjacency, not merit**, head of the cycle-10 queue) · `agent-frontmatter-fields-v2` (73 — **all three fields verified real**, blocker fully fell, deferred **on merit**: no turn-count data exists to pick a cap; **first time "re-open ≠ accept" was exercised**) · `pinned-roster-snapshots` (62 — single source not fetched, ADR-013 bars it) · `loop-machinery-self-audit` (54 — ADR-027 class elevation at 7 occurrences, raised and scored as required, rejected as duplicate of ADR-033).
- **Both cycle-8 written obligations discharged:** the never-before-exercised curator branch (a file in `ideas/` with no ledger row) ran for the first time on both re-opened candidates, and the ADR-027 step-0.5 class signal was raised rather than dropped.
- **Known failures, recorded not hidden:** the `advisor` tool was unavailable on all 4 attempts; `pinned-roster-snapshots` was judged without fetching its source; ADR-037's residual (undocumented agent behavior would be omitted from the derived tool list and fail silently) was **not** refuted — it would require instantiating the agent.
- **Landing:** shadow PR (`landing: pr`), branch `owl/evolve-2026-08-12`. `main` untouched. Circuit breaker: 3 accepted = cap; 1 gate failure (halt at 3); **0 carve-out contacts**.

### Session 2026-07-30 — cycle 6 + full MCP spec absorption + mcp-builder refresh (3 PRs merged to `main`)
- **Cycle 6 — autonomous /owl:evolve, 0 accepted (PR #6).** Fresh dual research: codex brief `research-vault/inbox/research-brief-2026-07-30.md` (**gpt-5**, 14 sources, 11 idea blocks; frontmatter `idea_count: 10` miscount, non-blocking) + scout live corroboration (`scout-notes-2026-07-30.md`: OpenAI handoff `input_filter`/`HandoffInputData`; Anthropic proportional effort-scaling tiers — both real, none fabricated). **All 11 brief ideas were aliases of already-decided ledger ids** → **0 accepted** (precedented, cf. 2026-07-26): every candidate a behavioral-claim re-run of a pattern the-owl already embodies. **1 rejected** — `independent-adversarial-review` = `adversarial-review-gate`: governance-of-the-gate = **NFR-SEC-1 carve-out** (confirmed, not re-opened). **10 deferred/deduped** — notably `effort-budgets-and-hard-stops` stays deferred (68): its live-verified Anthropic evidence is about scaling *subagent count*, a runtime spawner the-owl structurally lacks (hub-spoke, one-agent-per-phase). No ADR, no agent edit, nothing changed by the cycle itself; ledger got a cycle-6 prose block for next-cycle dedup (**no new table rows** — aliases correctly get none). Staleness review (ADR-017): handoff-contract (ADR-004) extended yesterday, not stale; role-ownership (ADR-009) still pays. Circuit breaker: 0 accepted ≤ cap 3; 0 carve-out violations.
- **Full MCP spec-site v2026-07-28 absorption (PR #7, human-directed).** Closed the gap where only the Architecture page had been ingested (commit d24c68f). **+54 source notes** (`research-vault/sources/mcp-*.md`) covering the entire modelcontextprotocol.io v2026-07-28 site — 15 docs, 31 specification pages, 8 extensions. **Vault sources 51 → 105** (`research-vault/index.md`). **Security-first coverage (the main gap):** full threat model in `mcp-docs-security-best-practices` with every MUST/SHOULD/MUST NOT verbatim (Confused Deputy, Token Passthrough, SSRF, State-Handle Hijacking, local-server compromise, OAuth-URL XSS/RCE, Mix-Up, CIMD) + complete OAuth 2.1 authorization spec + enterprise/client-credentials auth extensions. **NFR-SEC-2 held:** every page carried a benign "fetch llms.txt" banner — treated as DATA, quoted in a `> [!question]` callout, **not obeyed** (0 injected directives acted on). Ingest-only (scout lane): curator did not score; partial fetches (`mcp-docs-sdk`, `mcp-ext-client-matrix`) flagged in-note.
- **`@mcp-builder` refreshed with the v2026-07-28 deltas (PR #8, human-directed).** Derived from the ingest, filling what the partial update (d24c68f) missed — knowledge refresh grounded in primary sources, **no ADR** (matches the d24c68f `docs(mcp-builder)` precedent for spec-sync). Deprecations completed (**Roots + Sampling + Logging all deprecated, SEP-2577**; also `includeContext` SEP-2596 + OAuth DCR); **MRTR** (`InputRequiredResult` + `requestState`) replaces all server-initiated requests; stateless — no `initialize` handshake, per-request `_meta` version negotiation, `server/discover` mandatory, new error codes -32020/-32021/-32022; Streamable HTTP drops the GET stream + protocol sessions; `subscriptions/listen` replaces `resources/subscribe`; Tasks extension. **New 🔒 security/authorization block** (OAuth 2.1 + PKCE, no token passthrough `MUST NOT` with `aud` RFC 9068 + Resource Indicators RFC 8707, confused-deputy per-client consent, SSRF discovery blocklist, state-handle binding, local-server exec consent, authz-URL XSS/RCE) — final security gate stays with @sentinel/@guardian. `mcp-builder.meta.yaml` unchanged (generic, no conflict).
- **Landing:** all three merged to `main` in dependency order (#7 → #8 → #6); the only conflict (`research-vault/log.md`, both #6 and #7 appended) resolved keeping both dated blocks. `main` at `de5583a`; shadow branches deleted. No new ADR (total_decisions stays 19); knowledge-graph structurally unchanged (no agent added/removed).

### Cycle 2026-07-29 (cycle 5) — autonomous /owl:evolve (shadow PR)
- **Fresh dual research:** codex brief `research-vault/inbox/research-brief-2026-07-29.md` (13 sources, 12 idea blocks — model **gpt-5**; frontmatter miscounts `idea_count: 11`, an off-by-one recorded in the log) + scout live corroboration (`scout-notes-2026-07-29.md`: x1 AutoGen termination conditions first-class, x2 Anthropic multi-agent explicit stop conditions + retry-limit escalation, x3 handoff-uncertainty — all real, none fabricated).
- **1 accepted (provisional), 1 rejected, 3 deferred (net-new), 7 aliases of already-decided ideas.** Accepted: **`handoff-contract-uncertainty-fields` (78, provisional)** — see ADR-020. Rejected: **`adversarial-review-gate`** — its only net-new slice (materiality thresholds on the guardian/sentinel/challenger closure gate) is governance-of-the-gate = **NFR-SEC-1 carve-out**, and the three-review gate is already implemented (`/owl:evolve` L4). Deferred (net-new, below the ADR-015-haircut bar, each substantially embodied already): `explicit-termination-and-escalation` (68), `untrusted-content-boundary` (65), `durable-decisions-separate-from-working-memory` (64). Aliases (not re-litigated, see ledger): `explicit-role-charters`→`role-ownership`, `artifact-first-pipeline` base→`handoff-contract`, `coordinator-owned-routing`→`supervisor-specialists`, `single-agent-default`→`single-agent-first`, `context-budgeted-handoffs`→`context-budgeting`, `frontmatter-capability-scoping`→`agent-frontmatter-fields`/`least-privilege-tool-scopes`, `trajectory-evals-with-baselines`→`trajectory-evals`, `library-layering-by-stability`→`scope-based-agent-library`.
- **L1.5 grounding did real work:** confirmed `docs/conventions/handoff-contract.md` has 6 fields, **none** for uncertainty (the accepted atomic gap); confirmed only `sentinel.md` carries an explicit escalate/stop contract (grounds the `explicit-termination-and-escalation` defer); confirmed ADR-as-promotion + ledger `status` already deliver durable-vs-working memory separation (grounds the `durable-decisions` defer).
- **Change (ADR-020):** add a 7th mandatory field — **"Premissas & Questões em aberto"** (assumptions / open questions / evidence confidence, bullet-level, context-minimal) — to the handoff-contract convention. Extends ADR-004 (new suffixed id, not a re-litigation); per-agent rollout is incremental in future cycles (ADR-004→011 pattern). **1 file touched** (`docs/conventions/handoff-contract.md`, additive). Claim verified live (ADR-013): OpenAI Agents SDK handoffs carry structured `input_type`/`reason` payloads — `confirmed`.
- **Gate (guardian/sentinel/challenger): PASS × 3.** Guardian: no agent `.md` touched, additive, no prose↔meta drift. Sentinel: **0 carve-out contact by the cycle diff** (the pre-existing uncommitted `.claude/settings.json` change is session state, **excluded from the commit**), no injection (brief treated as data), no secrets. Challenger: real non-blocking finding — a mandatory field no agent implements yet risks convention↔practice divergence (the unenforced-prose failure class) → **fixed in-gate** by annotating the field as incremental-rollout + a falsifiable revisit trigger (reconsider if unadopted in ~3 cycles).
- **Staleness review (ADR-017 step 4.5):** examined the 2 oldest conventions — `handoff-contract` (ADR-004) actively extended this cycle (not stale); `role-ownership` (ADR-009) still pays (this cycle's #1 high-confidence idea re-confirms role charters remain SOTA). No re-fitness flagged.
- **Landing:** shadow PR (`landing: pr`, default) — branch `owl/evolve-2026-07-29-handoff-contract-uncertainty-fields`, not merged to `main`.

### Session 2026-07-27/28 — human-directed: token-economy measurement + Second Brain (graphify + Obsidian)
- **Agent token cost measured, not guessed (ADR-018):** real A/B subagent runs, ground-truth = the transcript jsonl (the agent's own "files I read" self-report was shown to be **inaccurate** — it claimed not to read the reference when the transcript proved it did). Established `cost = context-floor (~84–92k harness) × turn-count`, re-read every round-trip (cache_read). Ruled out by measurement, each <8%: agent file size (~1%), the `claude-architecture` skill (~1.3% marginal), MCP/tool surface (~8%); **`CLAUDE.md` is not loaded in subagents** (grep = 0 markers), only the main loop. **Turn-count is the only config-controllable lever.**
- **Turn-economy lever applied to the 4 leaned agents** (`architect`/`builder`/`chronicler`/`system-designer`, `f77e49c`): a `⚡ ECONOMIA DE TURNOS` block atop the critical rules + tightened reference pointers ("não leia por reflexo"). Measured before→after, same task each: **builder 22→6 turns −57%**, **architect 10→3 −49%**, **system-designer 9→3 (cache_read −85%) −37%**, **chronicler neutral** (already minimal — no harm). Quality preserved; the lean+lever beats even the big self-contained agent.
- **Second Brain — graphify + Obsidian (ADR-019):** turn any folder of code/docs into a queryable local knowledge graph. `docs/second-brain-atlas.md` (process, install, the asymmetric auto-update loop, global cross-project graph, security), `scripts/atlas-bootstrap.sh` (doctor/install/init), and the `/second-brain` skill (routing orchestrator over `/graphify`, shaped by the `claude-architecture` skill). Commits `1c7718e`/`5415911`/`144d435`/`d705413`.
- **One-vault decision (ADR-019):** the human vault is the existing curated **`research-vault`**; graphify is a **query-engine** (MCP/CLI/`graph.html`), never a second vault, never dumps auto-generated stubs into the curated vault. Default `init` = query-mode.
- **Validated live:** the-owl agents folder (13) → 54-node graph; `research-vault` (70 docs) → **246-node graph**; both merged into the global brain (`~/.graphify/global-graph.json`, **300 nodes**) — cross-project query proven. `research-vault` left 100% untouched.
- **Provenance (outside the repo):** the global `~/.claude/CLAUDE.md` (44.5k tokens, loaded every main-loop turn) was split into rules-only `CLAUDE.md` + `~/.claude/HISTORY.md` (dated changelog) — a ~43k-token/turn cut to the interactive loop. Noted for the record; the file lives outside the-owl.

### Cycle 2026-07-26 — autonomous /owl:evolve (shadow PR)
- **Fresh dual research:** codex brief `research-vault/inbox/research-brief-2026-07-26.md` (15 sources, 12 ideas — model `gpt-5.6-luna` again, same documented fallback; the brief's own `generator:` frontmatter inaccurately self-reports a different model, corrected in the vault) + scout live corroboration (`scout-notes-2026-07-26.md`, x1–x3, all independently verified real — none fabricated, including a specific arXiv id).
- **L1.5 grounding did real work again:** confirmed `.owl/state/` has no mid-cycle checkpoint (the accepted gap), confirmed `least-privilege-tool-scopes` has no enforcement path in the-owl's inline execution model (ADR-010) so its score moved **down** despite stronger evidence, and confirmed `evaluator-optimizer-loop` is now substantially covered by the fitness harness shipped 2026-07-25 (ADR-014/015).
- **1 accepted (provisional), 2 rejected, 2 aliases of already-decided ideas, 5 deferred.** Accepted: **`externalized-checkpoint-memory` (83 raw → 75 after the ADR-015 self-haircut)** — see ADR-016. Rejected: `trajectory-evals` (58 — its own best-cited source, read in full, argues the opposite of what it proposes) and `parallel-independent-work` (52 — no execution path in the current sequential loop, same shape as the earlier `isolated-workspaces` reject). Aliases (not re-litigated): `narrow-role-boundaries`→`role-ownership`, `structured-handoff-contracts`→`handoff-contract`.
- **Change (ADR-016):** a mid-cycle checkpoint for `/owl:evolve` (`.owl/state/cycle-in-progress.json`, written after each phase's existing verification, read at Setup with an explicit resume-or-fresh choice) — closes the ambiguity ADR-010 first flagged (a crashed cycle looks identical to "nothing ran yet"). **1 file touched** (`.claude/commands/owl/evolve.md`, additive only).
- **Gate (guardian/sentinel/challenger): PASS × 3** — 0 carve-out contact, no injection/secrets, additive-only. Challenger added a concrete revisit trigger: if the checkpoint is never found non-stale across ~5 cycles, that's grounds to remove it.
- **Landing:** shadow PR (`landing: pr`, default) — branch `owl/evolve-2026-07-26-externalized-checkpoint-memory`, not merged to `main`.

### 🦉 Milestone — Self-Improvement Loop LIVE (2026-07-24)
the-owl now improves its own agents autonomously, on a daily schedule, with a human merge gate.
- **Live & armed:** `/owl:evolve` runs headless **weekly, Mondays 07:13** (was daily until 2026-07-24 — the binding constraint is integration, not research; ADR-012) via launchd (`scripts/owl-daily.sh` + `com.evolvelabs.owl.daily.plist`), shadow-only by default (opens a PR; never auto-touches `main`).
- **3 autonomous cycles proposed, reviewed, and MERGED to `main`:** handoff-contract convention (ADR-004) → rolled into architect/builder/chronicler (ADR-006/007/008) → role-ownership convention (ADR-009). All PR branches cleaned up.
- **12 ADRs** (001–012) record every decision. Load-bearing invariant **NFR-SEC-1** (ADR-001): the loop may improve any agent **except its own guardrails** (sentinel veto, guardian gate, challenger, rubric safety floor, scope allow-list, `.owl/loop-config.yml`, schedule, `~/.ssh`, secrets) — those stay human-only. 0 carve-out contact across all cycles.
- **Proven behaviors:** dual research (codex + scout), rigorous scoring (it *rejected* `isolated-workspaces` at 41/100), gap-analysis against our own code (ADR-005), and reliable headless execution with per-phase output verification (ADR-010).

### Cycle 2026-07-24b — human-directed: finish the convention rollout + measure efficiency (committed to `main`)
- **Why now:** a new efficiency scorecard (`scripts/owl-metrics.py`, ADR-012) quantified the standing "convention debt" — `handoff-contract` on only 3/7 target agents, `role-ownership` on 0/7. The bottleneck is **integration, not research** (the same-day guard had already fired on daily cadence).
- **Rollout completed (ADR-011):** added the 6-field **"🤝 Contrato de Handoff"** to `curator`/`scout`/`strategist`/`system-designer` and the 5-field **"🧭 Papel & Não-Papel"** to all 7 pipeline specialists (`architect`/`builder`/`chronicler`/`curator`/`scout`/`strategist`/`system-designer`); `system-designer.meta.yaml` gained a `constraints` block. **176 insertions, 0 deletions.** Both conventions now **7/7 (100%)**.
- **`team` excluded as N/A by design:** it is the DevFlow parallel **orchestrator (hub)**, not a pipeline specialist — encoded as `NA_FOR_CONVENTIONS`, printed on its own line, not silently dropped. @challenger verdict: legitimate, "the honest denominator."
- **L1.5 grounding (ADR-005) corrected ADR-009:** `scout`/`curator` DO have `.meta.yaml` in the canonical `.devflow/agents/`; only `team` lacks one. The `.claude/commands/agents/*.meta.yaml` mirrors are drifted → tracked follow-up.
- **Efficiency (ADR-012):** shipped the read-only rollout-coverage scorecard + moved the schedule **daily → weekly (Mondays 07:13)**. Headline metric = *is accepted work finished across the fleet?* (not ADR/PR counts). Cost instrumentation = follow-up.
- **Gate (guardian/sentinel/challenger): PASS × 3** — additive-only + agent-specific, 0 carve-out contact, no injection/secrets, real improvement. **Landing: committed to `main`** (human-directed, attended, gate-reviewed — deliberate deviation from shadow-PR, since `gh` is absent and the directive was to finish it without a human merge step).

### Cycle 2026-07-24 — autonomous /owl:evolve (merged via PR #3)
- **Fresh dual research:** codex brief `research-vault/inbox/research-brief-2026-07-24.md` (22 sources, 16 ideas — default model `gpt-5.6-luna` via `~/.codex`; the deep-research model is not available on this ChatGPT account, so the skill's documented fallback was used) + scout live pass (`scout-notes-2026-07-24.md`, x1–x5).
- **L1.5 grounding (ADR-005) mattered:** scoring against the real code found ownership is already in `.meta.yaml` for **8/11** agents, but `scout`, `curator`, `sentinel` lack it — a concrete inconsistency (sentinel = carve-out, human-only).
- **1 accepted / 1 rejected / rest deferred** — accepted `role-ownership` (87/100, promotes the previously-deferred `explicit-role-boundaries` with today's stronger evidence); rejected `isolated-workspaces` (41 — runtime-shaped, applicability 2/5).
- **Change (merged):** **ADR-009** + `docs/conventions/role-ownership.md` (the "Papel & Não-Papel" ownership convention). Gate (guardian/sentinel/challenger) PASS; **0 carve-out contact**. Challenger flag (non-blocking): two conventions (handoff-contract, role-ownership) now await rollout — prioritize rollout over new conventions next cycle.

### Cycle 2026-07-23b — /owl:evolve continuation (merged via PR #2)
- **Same-day guard honored:** no new codex research spend — the cycle-1 brief was still fresh, so the loop processed the **queued backlog** instead of re-running L0.
- **First cycle to use L1.5 grounding (ADR-005):** audited the real agent files — architect/builder/chronicler had **no** handoff-contract section (only informal prose); scout/curator partial. This grounded the acceptance in the real gap.
- **1 accepted (94/100):** `handoff-contract-rollout` — the follow-up ADR-004 explicitly deferred. Rolled the "🤝 Contrato de Handoff" convention (6 fields) into the three integrate→land agents, **at the circuit-breaker cap (3)**.
- **Change:** ADR-006 (architect) + ADR-007 (builder) + ADR-008 (chronicler), each = 1 additive, agent-specific handoff-contract section. Gate (guardian/sentinel/challenger) **PASS** — additive-only, role boundaries preserved, no injection/secrets, 0 carve-out contact, real (not cargo-cult) improvement.
- **Queued next:** roll the same convention into scout/curator (partial) + strategist/system-designer; the deferred backlog ideas remain untouched (not re-litigated).

### Cycle 2026-07-23 — first autonomous /owl:evolve (merged via PR #1)
- **Dual research proven:** codex brief (ChatGPT-side) + scout live web research (Claude-side), merged/deduped by curator.
- **1 accepted / 9 deferred** — curator scored the merged set; accepted `handoff-contract` (91/100), rest deferred (evidence captured, not re-litigated).
- **Change (merged):** ADR-004 + `docs/conventions/handoff-contract.md` (standardized agent handoff contract). Gate (guardian/sentinel/challenger) PASS; 0 carve-out contact.

### Added — Self-Improvement Loop (planning + architecture)
- **PRD** `docs/planning/prd-owl-self-improvement.md` — the daily autonomous loop that lets the-owl improve its own agents from field research, with rigorous scoring and a safe self-modification model.
- **EPIC-001** `docs/planning/stories/EPIC-001-self-improvement-loop.md` — US-001…US-009, MoSCoW, dependency graph.
- **Artifacts** `docs/planning/artifacts/` — the ChatGPT research-brief prompt (8a) and its output schema (8b).
- **ADR-001** `docs/decisions/ADR-001-self-improvement-loop.md` — Accepted. The loop, the 3 locked decisions (auto-commit to `main` · codex-automated input · full guardian+sentinel+challenger gate), and the load-bearing safety invariant **NFR-SEC-1** (the loop cannot autonomously edit its own guardrails), shadow-first rollout.
- **ADR template** `docs/decisions/000-template.md` — was referenced but missing; created.

### Added — Implementation (@builder)
- **Git** — the-owl connected to `github.com/evolve-labs-cloud/the-owl` (branch `main`).
- **Vault** `research-vault/` — Obsidian external-research RAG (SCHEMA + index/log/overview/ledger + inbox/sources/patterns/ideas). See **ADR-002**.
- **scout** + **curator** agents — `.claude/commands/agents/{scout,curator}.md` + `.devflow/agents/*.meta.yaml`, registered in `project.yaml` (10 agents). scout researches; curator scores rigorously and owns the vault. Rubric in **ADR-003**.
- **Commands** — `/owl:research` (codex → daily brief) and `/owl:evolve` (the L0→L5 loop orchestrator; carve-out + circuit breaker enforced).
- **Schedule** — `scripts/owl-daily.sh` + launchd template `scripts/com.evolvelabs.owl.daily.plist` (human loads it once; not auto-installed).
- **Config** `.owl/loop-config.yml` — landing mode (default `pr`/shadow), circuit breaker, rubric threshold + safety floor.

### Done since
- ✅ First `/owl:evolve` run validated the codex output-capture flag + the full pipeline (verified `codex exec -o/--output-last-message`, committed in `bfcdbf7`).
- ✅ Daily schedule armed (`launchctl bootstrap`, `com.evolvelabs.owl.daily` loaded; first scheduled run fired 2026-07-24 07:13).
- ✅ Headless reliability hardened — inline phase execution + per-phase output verification (ADR-010), after the first scheduled run exposed a silent scout no-op.

### Known follow-ups (non-blocking)
- **Convention rollout > new conventions** (the loop's own challenger flag): roll handoff-contract + role-ownership into the remaining agents (scout/curator/strategist/system-designer; sentinel/guardian/challenger = human-only, carve-out).
- **codex deep-research model** unavailable on the account → falls back to `gpt-5.6-luna`. Point `research.model` at an available model if the deep-research tier is wanted.
- **Tokenless launchd PR-open:** scheduled runs push the branch but can't open the PR (no `gh`/token in that env) — a human opens it. ADR-010 records the branch + compare URL fallback.
- **Docs:** `docs/wiki/` not yet initialized; `.devflow/knowledge-graph.json` referenced in `project.yaml` but not present (regenerate via `/graph regenerate`).
- **Diagrams:** architecture diagrams added in `docs/architecture/diagrams/` (Mermaid).
