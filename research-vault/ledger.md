---
title: Decision Ledger
type: index
tags: []
updated: 2026-07-26
---

# Decision Ledger — dedup source of truth

Before scoring any candidate, `curator` checks this table. A decided `id` is **skipped** — the-owl never re-litigates a settled idea (see [[SCHEMA]] → The ledger). Materially new evidence for a decided idea gets a **new suffixed id**, never a silent overwrite.

`status`: `accepted` · `rejected` · `deferred` · `quarantined`

| id | title | score | status | adr | first_seen | decided |
|----|-------|-------|--------|-----|------------|---------|
| handoff-contract | Explicit handoff contract per agent | 91 | accepted | ADR-004 | 2026-07-23 | 2026-07-23 |
| handoff-contract-rollout | Roll the handoff contract into individual agents (architect/builder/chronicler) | 94 | accepted | ADR-006, ADR-007, ADR-008 | 2026-07-23 | 2026-07-23 |
| role-ownership | Role ownership & non-ownership convention | 87 | accepted | ADR-009 | 2026-07-23 | 2026-07-24 |
| explicit-role-boundaries | Standardized role/non-goals section | 84 | accepted | ADR-009 | 2026-07-23 | 2026-07-24 |
| isolated-workspaces-for-parallel-coding | File ownership / worktree isolation | 41 | rejected | | 2026-07-24 | 2026-07-24 |
| least-privilege-tool-scopes | Tight per-agent tool lists | 66 | deferred | | 2026-07-23 | 2026-07-26 |
| evaluator-optimizer-loop | Grader/rubric return-gate | 68 | deferred | | 2026-07-23 | 2026-07-26 |
| provenance-first-evaluation | Cite a real source per claim | — | deferred | | 2026-07-23 | 2026-07-23 |
| artifact-oriented-context | Durable artifacts over chat history | — | deferred | | 2026-07-23 | 2026-07-23 |
| markdown-source-of-truth | Single markdown source of truth | — | deferred | | 2026-07-23 | 2026-07-23 |
| workflow-first-orchestration | Deterministic workflow + agent at decision points | — | deferred | | 2026-07-23 | 2026-07-23 |
| centralized-governance-hub | Central governance/hub doc | — | deferred | | 2026-07-23 | 2026-07-23 |
| agent-frontmatter-fields | maxTurns/Memory/isolation frontmatter | — | deferred | | 2026-07-23 | 2026-07-23 |
| manager-retains-control | Central manager, bounded specialists | — | deferred | | 2026-07-24 | 2026-07-24 |
| sop-as-executable-contract | SOP as staged workflow contract | — | deferred | | 2026-07-24 | 2026-07-24 |
| scope-based-agent-library | Scope-aware folder organization | — | deferred | | 2026-07-24 | 2026-07-24 |
| context-isolation-and-summary | Fresh context + summary per specialist | — | deferred | | 2026-07-24 | 2026-07-24 |
| durable-state-separated-from-transcript | Durable artifact classes vs transcript | — | deferred | | 2026-07-24 | 2026-07-24 |
| evidence-action-observation-loop | Evidence log / ReAct-style grounding | — | deferred | | 2026-07-24 | 2026-07-24 |
| human-approval-at-side-effect-boundaries | Approval gate at side-effects | — | deferred | | 2026-07-24 | 2026-07-24 |
| directed-handoff-graph | Directed edges, no free mesh | — | deferred | | 2026-07-24 | 2026-07-24 |
| adr-backed-prompt-evolution | Extend ADR template for prompt changes | — | deferred | | 2026-07-24 | 2026-07-24 |
| handoff-role-rollout-completion | Finish handoff-contract + role-ownership across all pipeline agents | — | accepted | ADR-011 | 2026-07-24 | 2026-07-24 |
| efficiency-scorecard | Rollout-coverage scorecard + weekly cadence | — | accepted | ADR-012 | 2026-07-24 | 2026-07-24 |
| just-in-time-context-loading | Load context/tools on demand via lightweight pointers; progressive disclosure (metadata→body→files) | — | deferred | | 2026-07-26 | 2026-07-26 |
| convention-staleness-review | Curator re-examines old conventions; flags stale ones for owner-reviewed re-fitness | 82 | accepted | ADR-017 | 2026-07-26 | 2026-07-26 |
| eval-saturation-graduation | Graduate saturated capability evals into the regression suite; keep regression ~100% | — | deferred | | 2026-07-26 | 2026-07-26 |
| externalized-checkpoint-memory | Explicit state and resumable checkpoints (mid-cycle) | 75 | accepted | ADR-016 | 2026-07-26 | 2026-07-26 |
| context-budgeting | Structured Context Budget section per agent | 74 | deferred | | 2026-07-26 | 2026-07-26 |
| single-agent-first | "Why multiple agents?" field on team-design ADRs | 69 | deferred | | 2026-07-26 | 2026-07-26 |
| supervisor-specialists | Formalize orchestrator-as-sole-delegator in every agent | 67 | deferred | | 2026-07-26 | 2026-07-26 |
| sequential-artifact-pipeline | Canonical enumerated stage-artifact list | 68 | deferred | | 2026-07-26 | 2026-07-26 |
| human-approval-gates | Per-agent Approval Gate block | 67 | deferred | | 2026-07-26 | 2026-07-26 |
| trajectory-evals | Mandatory per-agent trajectory-check eval contract | 58 | rejected | | 2026-07-26 | 2026-07-26 |
| parallel-independent-work | Parallelization-eligibility field on ADRs | 52 | rejected | | 2026-07-26 | 2026-07-26 |
| handoff-contract-uncertainty-fields | Add assumptions / open-questions / evidence-confidence fields to the handoff contract | 78 | accepted | ADR-020 | 2026-07-29 | 2026-07-29 |
| explicit-termination-and-escalation | Per-agent Stop/Escalate/Approval contract | 68 | deferred | | 2026-07-29 | 2026-07-29 |
| untrusted-content-boundary | Evidence Trust section; handoffs carry no new authority | 65 | deferred | | 2026-07-29 | 2026-07-29 |
| durable-decisions-separate-from-working-memory | Memory Promotion section (durable vs working memory) | 64 | deferred | | 2026-07-29 | 2026-07-29 |
| adversarial-review-gate | Closure gate with materiality thresholds | — | rejected | | 2026-07-29 | 2026-07-29 |

> Cycle 2026-07-23: circuit breaker cap = 3 accepted/cycle; cycle 1 kept conservative → **1 accepted**, rest **deferred** (revisit next cycles, evidence already captured; not re-litigated).
>
> Cycle 2026-07-23b (continuation, no new codex spend): applied the new L1.5 grounding (ADR-005) to the queued **handoff-contract rollout** — 3 accepted edits (architect/builder/chronicler) = the ADR-004 follow-up, at the cap. scout/curator (partial) + strategist/system-designer queued next. Deferred backlog ideas untouched (not re-litigated). Merged via PR #2.
>
> Cycle 2026-07-24: fresh codex brief (22 sources / 16 ideas) + scout live pass. Deduped vs ledger + **L1.5 grounded against the real code**. **1 accepted** (`role-ownership` / ADR-009 — promotes the previously-deferred `explicit-role-boundaries` with today's stronger evidence, now accepted under ADR-009), **1 rejected** (`isolated-workspaces` — runtime-shaped, applicability 2/5), rest **deferred**. Brief-id aliases (same idea, not re-litigated): `narrow-single-owner-roles`→`role-ownership`; `typed-minimal-handoffs`→`handoff-contract`; `tool-allowlists-and-provenance`→`least-privilege-tool-scopes`; `evaluator-optimizer-with-replay`→`evaluator-optimizer-loop`; `deterministic-workflow-before-autonomous-swarm`→`workflow-first-orchestration`; `markdown-frontmatter-config`→`agent-frontmatter-fields`. Several deferred ids (`manager-retains-control`, `directed-handoff-graph`, `human-approval-at-side-effect-boundaries`, `adr-backed-prompt-evolution`) are **already largely implemented** in the-owl (hub-spoke, HITL, ADR-per-change) → deferred with that noted, low re-accept priority.
>
> Cycle 2026-07-26 **(score-only backlog pass, no new codex spend):** scored the **24 read-in-full sources** imported 2026-07-26 (the Jul-26 library, incl. the carinhAI `Engineering/AI/` docs) that were ingested-but-unscored. Deduped vs ledger + **L1.5 grounded** against the real agents/conventions. **Accepted: 0** — the expected outcome under the ADR-015 self-discount (~+15 curator optimism) on a set where every candidate is a *behavioral-claim* idea (Impact is hypothesis-level, not ceiling); the cap of 3 is a ceiling, not a target. **3 net-new candidates recorded as deferred** (`just-in-time-context-loading`, `convention-staleness-review`, `eval-saturation-graduation`). Everything else **deduped/corroborated** existing decisions:
> - `just-in-time-context-loading` → **deferred**: its core (pass pointers, load on demand) is already the-owl's practice via **`handoff-contract`** (accepted, ADR-004 — context-minimal, "só o necessário + paths") and progressive disclosure is already latent in `.claude/skills`; strongly corroborated by 4 new primaries ([[effective-context-engineering]], [[anthropic-agent-skills]], [[advanced-tool-use]], [[code-execution-mcp]]) but no concrete atomic gap in a markdown-only/no-runtime lib.
> - `convention-staleness-review` → **deferred**: genuine gap (ADR-014/015 fitness-gate *new* conventions at accept-time; nothing re-tests *old* ones as models improve). Corroborated by [[harness-design-long-running-apps]] + [[scaling-managed-agents]] ("harness assumptions go stale"). Deferred for a **carve-out-safe** focused cycle — its proposed_change must edit the curator flow, **NOT** `.owl/loop-config.yml` (NFR-SEC-1).
> - `eval-saturation-graduation` → **deferred**: net-new eval methodology from [[anthropic-demystifying-evals]]; touches `eval/` (ADR-014) → careful, low-urgency cycle.
> - **Corroboration (no new id):** [[anthropic-building-effective-agents]] → `handoff-contract`/`role-ownership`/`evaluator-optimizer-loop`/`manager-retains-control`/`human-approval-at-side-effect-boundaries`. [[multi-agent-research-system]] + [[scaling-managed-agents]] → hub-spoke + `durable-state-separated-from-transcript` (+ **caution**: multi-agent economics — "unsuitable for parallelization-poor domains like most coding"). [[effective-harnesses-long-running]] + [[effective-context-engineering]] → `artifact-oriented-context`/`context-isolation-and-summary`/`agent-frontmatter-fields`. [[anthropic-demystifying-evals]] + [[harness-design-long-running-apps]] → `evaluator-optimizer-loop` + confirm the-owl's outcome-grading judge is consistent.
> - **Low fit for a markdown-only / no-runtime lib (noted, not new ids):** [[claude-code-sandboxing]], [[claude-code-auto-mode]], [[desktop-extensions]], [[advanced-tool-use]], [[code-execution-mcp]], [[claude-think-tool]], [[writing-tools-for-agents]], [[swe-bench-sonnet]], [[infrastructure-noise-evals]], [[eval-awareness-browsecomp]] — runtime/tooling/eval-infra depth; enrich the synthesis, propose no the-owl convention. [[ai-2027]] → cautionary input to [[self-improvement]]/[[guardrails]], not an actionable edit.
> - **Remaining three sources (recorded for completeness — no new id):** [[spdd-structured-prompt-driven-development]] → corroborates deferred `adr-backed-prompt-evolution` + `markdown-source-of-truth` (prompt-as-versioned-spec + two-way sync ≈ the-owl's ADR/prompt discipline; no atomic gap). [[building-c-compiler]] (16 parallel Claudes, git task-locking, role specialization) → **corroborates the rejection** of `isolated-workspaces-for-parallel-coding` (runtime-shaped, not markdown-only) + reinforces [[role-decomposition]]. [[ai-resistant-technical-evals]] → low fit (eval-design/harness depth, like the other eval sources); enriches the picture, proposes no the-owl convention.
> - **Curator boundary respected:** scored + persisted to the vault only. No agent/ADR edited, nothing committed. The 3 deferred candidates are future integrate-candidates, not applied changes.
>
> Integrate 2026-07-26 **(human-directed):** `convention-staleness-review` promoted **deferred → accepted (82)** and integrated as **ADR-017**. Promotion is rubric-justified (not by the direction): it reduced to a **concrete, carve-out-safe, data-independent** atomic edit (curator-judgment flag that *triggers* re-fitness — the aspirational "read a decayed Δ" form was rejected as it depends on un-instrumented data), claim **verified** live (ADR-013), and it is a **structural/process** convention (ADR-012 shape) so the ADR-015 behavioral discount barely applies. Edit: additive step 4.5 in `.claude/commands/agents/curator.md` (outside the carve-out). Impact **provisional-pending-first-flag**. **Landing: working-tree only** — gate (challenger independent + sentinel/guardian inline) then owner picks shadow-branch/PR; **not** committed to main by the loop.
>
> Cycle 2026-07-29 **(cycle 5):** fresh codex brief (13 sources, 12 idea blocks — frontmatter miscounts `idea_count: 11`; model gpt-5) + scout live corroboration (x1 AutoGen termination, x2 Anthropic multi-agent stop/escalation, x3 handoff-uncertainty — all real). **1 accepted, provisional** (`handoff-contract-uncertainty-fields`, 78 → ADR-020): the one concrete atomic gap — the accepted handoff contract (ADR-004) has **no field for assumptions / open questions / evidence confidence**, which two independent high-confidence brief ideas (`artifact-first-pipeline`, `context-budgeted-handoffs`) + verified primaries (OpenAI handoffs `input_type`/`reason`) converge on. New suffixed id — **extends** the convention, does not re-litigate ADR-004. Edits `docs/conventions/handoff-contract.md` only (atomic; per-agent rollout deferred to later cycles per the convention's own rule); not the carve-out. **1 rejected** (`adversarial-review-gate` — its net-new slice, materiality thresholds on the guardian/sentinel/challenger closure gate, is **governance-of-the-gate = NFR-SEC-1 carve-out**; also already implemented as `/owl:evolve` L4). **3 deferred, net-new** (`explicit-termination-and-escalation` 68, `untrusted-content-boundary` 65, `durable-decisions-separate-from-working-memory` 64) — each below the (ADR-015-haircut) bar and substantially embodied already (HARD STOP + circuit breaker; NFR-SEC-2 + sentinel; ADR-as-promotion + ledger status). **7 aliases of already-decided ids, not re-litigated:** `explicit-role-charters`→`role-ownership` (ADR-009); `artifact-first-pipeline` base→`handoff-contract` (ADR-004, only its uncertainty sub-slice is new); `coordinator-owned-routing`→`supervisor-specialists`/hub-spoke (ADR-010); `single-agent-default`→`single-agent-first`; `context-budgeted-handoffs`→`context-budgeting` (corroborates the accept); `frontmatter-capability-scoping`→`agent-frontmatter-fields`+`least-privilege-tool-scopes`; `trajectory-evals-with-baselines`→`trajectory-evals` (rejected); `library-layering-by-stability`→`scope-based-agent-library`. **Staleness review (ADR-017 step 4.5):** examined the 2 oldest conventions — `handoff-contract` (ADR-004) is **actively being extended this cycle**, not stale; `role-ownership` (ADR-009) **still pays** — this cycle's #1 high-confidence brief idea (`explicit-role-charters`) independently re-confirms non-overlapping role charters remain SOTA, so the current model has NOT made it native. No re-fitness flagged. Circuit breaker: 1 accepted ≤ cap 3.
>
> Cycle 2026-07-26: fresh codex brief (15 sources / 12 ideas, model gpt-5.6-luna — same fallback as before; the brief's own `generator:` frontmatter inaccurately self-reports a different model, corrected in `log.md`) + scout live corroboration (3 fetches, all real, none fabricated). **1 accepted, provisional** (`externalized-checkpoint-memory`, score 83 raw → **75 after the ADR-015 self-haircut** — the calibration probe's measured +15.4 curator optimism bias applies here; a genuinely-verified gap — `.owl/state/` confirmed to have no mid-cycle checkpoint — but still marginal-band, so the accept is explicitly provisional, revisit if unused). **2 rejected**: `trajectory-evals` (58 — its own best-cited source, read in full, argues the *opposite* of what the idea proposes: outcome-primary grading, not mandatory trajectory-checks; the-owl's ADR-014/015 harness already does the better-supported version) and `parallel-independent-work` (52 — ADR-010 explicitly documents the loop as sequential/one-agent-per-phase, same "presupposes a runtime the-owl doesn't have" shape as 2026-07-24's `isolated-workspaces` reject). **2 aliases of already-decided ideas, not re-scored** (brief generated fresh ids for existing decisions): `narrow-role-boundaries` → `role-ownership` (ADR-009, accepted); `structured-handoff-contracts` → `handoff-contract` (ADR-004, accepted). **2 re-scored deferred ideas with a materially-changed basis:** `evaluator-optimizer-loop` (unscored → 68 — the-owl's own ADR-014/015 fitness harness now substantially implements this; no further action needed, not a fresh gap) and `least-privilege-tool-scopes` (78 → **66, revised down** despite stronger evidence this cycle — grounding found the-owl's inline, non-subagent execution model (ADR-010) has **no mechanism to actually restrict which tools are available per-agent**, so a "Forbidden Tools" section would be unenforceable prose with a false-confidence risk, same failure class as the rejected `isolated-workspaces`; Fit dropped more than Evidence rose). **5 new deferred** (`context-budgeting` 74, `single-agent-first` 69, `supervisor-specialists` 67, `sequential-artifact-pipeline` 68, `human-approval-gates` 67) — each already substantially embodied in the-owl's existing conventions/architecture (verified directly per-candidate, not assumed), or landing just under the self-haircut-adjusted bar.
>
> Cycle 2026-07-30 **(cycle 6):** fresh codex brief (gpt-5, 14 sources, 11 idea blocks — frontmatter `idea_count: 10` miscounts, non-blocking) + scout live corroboration (2 fetches, both real, none fabricated: OpenAI Agents SDK handoff `input_filter`/`HandoffInputData`; Anthropic multi-agent proportional effort-scaling tiers). **0 accepted** — precedented + expected (cf. 2026-07-26, also 0-accepted): **all 11 brief ideas are aliases of already-decided ledger ids**, every candidate a behavioral-claim re-run of a pattern the-owl already embodies. Cap (3) is a ceiling not a target; weekly-cadence rationale (absorption, not research supply, is binding) agrees — yesterday's ADR-020 has not had its week. No accept manufactured. **1 rejected** (`independent-adversarial-review` = `adversarial-review-gate`, rejected 2026-07-29 as governance-of-the-gate on guardian/sentinel/challenger = **NFR-SEC-1 carve-out**; also already `/owl:evolve` L4 — confirmed, not re-opened). **11 aliases of already-decided ids, not re-litigated:** `role-boundaries-and-artifact-contracts`→`role-ownership` (ADR-009)+`handoff-contract` (ADR-004); `coordinator-owned-hub-spoke`→hub-spoke (ADR-010); `yaml-frontmatter-and-prompt-body`→`agent-frontmatter-fields` (deferred); `least-privilege-tool-scoping`→`least-privilege-tool-scopes` (deferred 66, unenforceable-prose risk unchanged in inline exec model); `structured-handoff-contracts`→`handoff-contract` (ADR-004+ADR-020); `n-minus-one-context-transfer`→`context-budgeting` (deferred 74; OpenAI `input_filter` corroborates but is runtime, no markdown gap); `adr-as-durable-decision-memory`→`durable-decisions-separate-from-working-memory` (deferred 64); `evidence-gated-evaluation-loop`→fitness harness (ADR-014/015), its trajectory-eval slice stays `trajectory-evals` **rejected 58** (best-cited source argues outcome-primary grading, the opposite); `effort-budgets-and-hard-stops`→`explicit-termination-and-escalation` (**deferred 68, stays**); `human-approval-at-boundary-crossings`→`human-approval-gates` (deferred 67, already embodied by HARD STOP + HITL + carve-out human gate + `landing: pr`). **Key non-promotion:** `effort-budgets-and-hard-stops` — live-verified Anthropic evidence ("1 agent with 3-10 tool calls" / "2-4 subagents" / ">10 subagents", 15× tokens) is about **scaling subagent COUNT**, a runtime spawner the-owl structurally lacks (hub-spoke, one-agent-per-phase); same runtime-shaped/low-fit class as rejected `isolated-workspaces`/`parallel-independent-work`. Evidence does NOT match the recorded revisit condition ("a single 'Stop When' line distilled into the handoff convention"); re-scoring one day after deferral would be re-litigation. **Staleness review (ADR-017 step 4.5):** `handoff-contract` (ADR-004) extended yesterday (ADR-020), not stale — its incremental per-agent uncertainty-field rollout is the live follow-up; `role-ownership` (ADR-009) **still pays** — today's #1 idea (`role-boundaries-and-artifact-contracts`) independently re-confirms non-overlapping artifact-owning role charters remain SOTA (Anthropic multi-agent + LangChain + Claude Code subagents converge); current model has NOT made it native. No re-fitness flagged. Circuit breaker: 0 accepted ≤ cap 3; 0 gate failures; 1 carve-out contact auto-rejected, 0 violations. No ADR, no edit, nothing landed (L2.5/L3/L4/L5 not reached). Vault memory persisted (brief + scout-notes + log + this block) as working-tree changes.

> Cycle 2026-08-03 **(cycle 7):** fresh codex brief (gpt-5, 13 sources, 10 idea blocks) + scout live corroboration (1 net-new source registered — SWE-bench paper arXiv:2310.06770; the Claude Code subagents doc confirms `disallowedTools`/`permissionMode`/`maxTurns` are REAL harness-**enforced** frontmatter fields; AutoGen's 11 termination conditions verified live; all URLs real, 0 fabricated; 3 repo star-counts unconfirmed but repos confirmed real). **0 accepted** — 4th consecutive 0-accept, precedented (cf. 2026-07-26, 2026-07-30); cap (3) is a ceiling not a target, none manufactured. **4 aliases of already-decided ids, not re-litigated:** `bounded-role-charters`→`role-ownership` (ADR-009, 87); `structured-handoff-contracts`→`handoff-contract` (ADR-004, 91); `hub-spoke-return-control`→hub-spoke (ADR-010); `eval-backed-library-evolution`→fitness harness (ADR-014/015). **5 deferred, substantially embodied:** `stage-gated-sop-pipeline`→`sequential-artifact-pipeline` (68, embodied in /quick:* + /owl:evolve stage gates); `scoped-context-artifacts`→`context-budgeting` (74); `append-only-decision-memory`→`durable-decisions-separate-from-working-memory` (embodied by ADR + ledger); `layered-guardrails-and-approval`→`human-approval-gates` (67, embodied by HARD STOP + `landing: pr` + carve-out human gate); `deterministic-termination-budgets`→`explicit-termination-and-escalation` (68 — its turn-budget slice IS now markdown-expressible per the scout's verified AutoGen finding, distinct from cycle-6's rejected agent-count-scaling framing; re-scored ~71 raw, still below threshold 75 vs the existing per-agent HARD STOP blocks, and the ADR-015 behavioral haircut pushes it lower → stays deferred, not ceremony this cycle). **Key state-change (NOT a loop accept):** `least-privilege-tool-scopes` (66, deferred 2026-07-23 as \"unenforceable prose in the-owl's inline-exec model, ADR-010\") + `agent-frontmatter-fields` (deferred) — the scout confirmed `tools`/`disallowedTools` are REAL harness-enforced subagent frontmatter fields, and the-owl now has a **human-directed** branch `owl/agents-native-subagents` (this session) adding native `.claude/agents/*.md` subagents WITH enforced `tools:` scoping (hybrid: 13 subagents + 13 command personas; pack owl-agents mirrors 11+11). The deferral blocker (inability to enforce per-agent tool scope) is therefore being removed — but **outside the loop, by owner direction, not by /owl:evolve** — and the tight-tool-list slice on `sentinel`/`guardian`/`challenger` is **NFR-SEC-1 carve-out** (the loop never edits them; the migration scoped those three by human hand). The loop does NOT accept/edit; it **records** the transition: `least-privilege-tool-scopes` deferred → **in-progress (human-directed, on branch, pending merge)**. Re-open for a loop accept only if a concrete carve-out-safe atomic slice for a NON-carve-out agent still remains after the migration merges. **Staleness review (ADR-017 step 4.5):** `handoff-contract` (ADR-004) — actively extended (ADR-020 uncertainty-field rollout ongoing), not stale; `role-ownership` (ADR-009) — still pays, today's `bounded-role-charters` (brief's #1) independently re-confirms non-overlapping role charters remain SOTA (Anthropic multi-agent + LangGraph + Claude Code subagents converge). No re-fitness flagged. Circuit breaker: 0 accepted ≤ cap 3; 0 gate failures; 0 carve-out violations (1 carve-out contact on the tool-scoping slice, correctly NOT acted on by the loop). L2.5/L3/L4/L5-integrate not reached (nothing accepted). Vault research memory persisted; landing via shadow (main untouched).
