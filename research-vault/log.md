---
title: Cycle Log
type: log
tags: []
updated: 2026-07-23
---

# Cycle Log (append-only)

One block per cycle event: `ingest` (scout), `score` (curator), `integrate` (handshake), `lint`.

## [2026-07-23] scaffold | Vault created
- Created: SCHEMA.md, index.md, overview.md, ledger.md, log.md, inbox/, sources/, patterns/, ideas/

## [2026-07-23] ingest | Cycle 1 — dual research (codex + scout)
- L0 codex brief: `inbox/research-brief-2026-07-23.md` (8 sources, 9 ideas; generator gpt-5-deep-research).
- L1 scout live research: `inbox/scout-notes-2026-07-23.md` (w1–w4). Corroborated 5 codex ideas; added frontmatter-fields + AutoGen-maintenance-mode fact; flagged star-count contradiction.

## [2026-07-23] score | Cycle 1 — curator
- Deduped vs ledger (empty). Scored merged candidate set.
- Accepted: **handoff-contract** (91). Deferred: 9 others (circuit-breaker cap = 3; cycle 1 conservative → 1 accepted).
- Safety veto applied; none touched the carve-out.

## [2026-07-23] integrate | Cycle 1 — ADR-004 + convention
- @architect wrote ADR-004; created `docs/conventions/handoff-contract.md`.
- Gate (guardian/sentinel/challenger): PASS (no role break, no injection/carve-out/secret, real improvement; challenger deferred-impact caveat noted).
- Landing: **shadow (pr)** → branch `owl/evolve-2026-07-23-handoff-contract`; main untouched.

## [2026-07-23] score | Cycle 2 (continuation) — curator
- **No new codex spend** (same-day guard): brief from cycle 1 still fresh; processed the queued backlog instead of re-running L0.
- L1.5 self-audit (ADR-005, first cycle to have it) on the real agent files: architect/builder/chronicler have **no** handoff-contract section (only informal prose); scout/curator partial.
- Accepted: **handoff-contract-rollout** (94) — the ADR-004 follow-up, 3 edits (architect/builder/chronicler) at the circuit-breaker cap. Deferred backlog not re-litigated.

## [2026-07-23] integrate | Cycle 2 — ADR-006/007/008 + agent edits
- @architect wrote ADR-006 (architect), ADR-007 (builder), ADR-008 (chronicler).
- @builder added a "🤝 Contrato de Handoff" section (the 6 convention fields, agent-specific) to each of the 3 agent files. Additive; existing prose + Skill-tool chaining preserved.
- Gate (guardian/sentinel/challenger): **PASS** — additive-only/role-preserved (guardian), 0 carve-out contact + no injection/secrets (sentinel), real improvement not cargo-cult (challenger). 0 consecutive failures.
- Landing: **shadow (pr)** → branch `owl/evolve-2026-07-23-handoff-rollout`; merged via PR #2.

## [2026-07-24] ingest | Cycle 3 — dual research (codex + scout)
- L0 codex brief: `inbox/research-brief-2026-07-24.md` (22 sources, 16 ideas; default model gpt-5.6-luna via ~/.codex — deep-research model unavailable on the ChatGPT account, fell back per skill).
- L1 scout live research: `inbox/scout-notes-2026-07-24.md` (x1–x5). Strengthened narrow-single-owner-roles / manager-retains-control / sop / typed-handoffs; weakened isolated-workspaces (no parallel writers); flagged already-implemented dedup signals.

## [2026-07-24] score | Cycle 3 — curator (L1.5 grounded)
- L1.5 self-audit vs real code: ownership already in `.meta.yaml` for 8/11 agents; **scout/curator/sentinel lack it** (sentinel = carve-out, human-only).
- Deduped vs ledger (6 brief-ids aliased to existing decided ids, not re-litigated).
- Accepted: **role-ownership (87)** → `docs/conventions/role-ownership.md` (promotes previously-deferred `explicit-role-boundaries`). Rejected: **isolated-workspaces (41)** (runtime-shaped, fit 2/5). Deferred: rest (evidence captured; several already implemented).
- Safety veto applied; the accepted change does NOT touch the NFR-SEC-1 carve-out. Circuit breaker: 1 accepted ≤ cap 3.

## [2026-07-24] integrate | Cycle 3b (human-directed) — complete the convention rollout + efficiency tooling
- **Trigger:** the new efficiency scorecard (`scripts/owl-metrics.py`, ADR-012) quantified the standing "convention debt": handoff-contract on 3/7 target agents, role-ownership on 0/7. Bottleneck = integration, not research.
- **L1.5 grounding (ADR-005):** corrected ADR-009's stale claim — `scout`/`curator` DO have `.meta.yaml` in the canonical `.devflow/agents/`; only `team` lacks one (N/A, orchestrator hub). Found the `.claude/commands/agents/*.meta.yaml` mirrors drifted (follow-up).
- **Change (ADR-011):** "🤝 Contrato de Handoff" added to curator/scout/strategist/system-designer; "🧭 Papel & Não-Papel" added to all 7 pipeline specialists; `system-designer.meta.yaml` gained `constraints`. **176 insertions, 0 deletions.** `team` excluded as N/A by design. Both conventions now **7/7 (100%)**.
- **Also (ADR-012):** shipped `scripts/owl-metrics.py` (read-only scorecard) + moved the schedule daily → **weekly (Mondays 07:13)**.
- **Gate (guardian/sentinel/challenger): PASS × 3** — additive-only/agent-specific (guardian), 0 carve-out contact + no injection/secrets (sentinel), real improvement + `team` exclusion honest, not goalpost-moving (challenger). 0 consecutive failures.
- **Landing: committed to `main`** — deliberate deviation from shadow-PR: human-directed, attended, gate-reviewed (see ADR-011 → Notas).

## [2026-07-24] lint | Sources layer materialized + first vault health pass
- **Materialized `sources/`** (was empty — the SCHEMA INGEST step never ran; the scout no-op'd): **25 source notes**, one per ingested codex-brief source across both cycles (deduped). name/URL/stars copied **verbatim** from `research-brief-2026-07-{23,24}.md` — no fabrication (`n/a` stays `n/a`; dual-cycle star counts noted). Scout sources (w1–w4, x1–x5) were already captured in the `type: source` scout-notes.
- **Wired provenance:** idea/pattern `## Related` now link the `sources/` notes (opaque `s#`/`x#`/`w#` IDs replaced by real wikilinks); the two briefs are **de-orphaned** (every source note links its origin `research-brief-*`); `overview.md` gains a `## Sources` hub linking all 25.
- **Health:** wikilinks **47 → 177**. **0 real broken links** (the 5 flagged are SCHEMA.md's own doc placeholders). **0 orphan notes** (was 2 briefs + 7 deferred-only sources — all now anchored).
- **Findings / next-cycle:** (1) 7 sources are cited only by *deferred* ideas → they gain idea-side inbound links when those ideas materialize; (2) `.meta.yaml` still carry stale paths (`docs/CHANGELOG.md`, `knowledge-graph.json`) → separate meta-accuracy pass; (3) no dedicated `patterns/` page for communication/handoff yet (only `role-decomposition`).

## [2026-07-26] score | Human-directed backlog pass (parallel to scheduled cycle 4) — read-in-full library
- **Scope:** the 24 **read-in-full** sources imported 2026-07-26 (the Jul-26 library + the carinhAI `Engineering/AI/` docs) were **ingested but never scored** (INGEST ≠ SCORE). This cycle mines + decides them. Deduped vs `ledger.md` + **L1.5 grounded** against the real agents/conventions.
- **Accepted: 0. Deferred (net-new): 3** (`just-in-time-context-loading`, `convention-staleness-review`, `eval-saturation-graduation`). Rest **deduped/corroborated** decided/deferred ids. Accept-0 is the expected outcome under the **ADR-015 self-discount** (~+15 curator optimism) on an all-behavioral-claim candidate set (Impact = hypothesis-level, not ceiling); the cap of 3 is a ceiling, not a target.
- **Why the strongest candidate didn't clear:** `just-in-time-context-loading`'s core (pass pointers, load on demand) is **already** the-owl's practice via `handoff-contract` (ADR-004, context-minimal) → dedup, not a new accept. `convention-staleness-review` is genuinely novel but hypothesis-level + **loop-adjacent** → deferred for a carve-out-safe cycle (edit curator flow, never `.owl/loop-config.yml`).
- **Safety veto:** n/a (nothing accepted); the 3 deferrals are flagged for carve-out care at any future integrate. **Curator boundary held:** scored + persisted to the vault only — no agent/ADR edited, nothing committed.
- **Synthesis:** [[overview]] refreshed (primary-source corroboration of context-minimal/hub-spoke/evaluator-gate + a multi-agent-economics caution + new deferred fronts); new `patterns/context-engineering.md` (partially closes the cycle-3 lint finding on missing pattern pages); [[index]] `[!todo]` (unscored library) resolved. Full dedup/corroboration map in `ledger.md`.

## [2026-07-26] integrate | Human-directed (parallel to scheduled cycle 4) — ADR-017 (convention staleness review)
- **Trigger:** owner directed integrating the strongest deferred candidate from the same-day score pass. Promoted `convention-staleness-review` **deferred → accepted (82)** — rubric-justified (not by the direction): concretized to an actionable, carve-out-safe, data-independent atomic edit; claim verified live (ADR-013: "harnesses encode assumptions... that go stale as models improve", Anthropic Managed Agents, fetched 2026-07-26); structural/process convention (ADR-012 shape), so the ADR-015 behavioral discount barely applies.
- **Change (ADR-017):** additive **step 4.5** in `.claude/commands/agents/curator.md` "🔄 Meu fluxo" — each cycle the curator re-reads the 1–2 oldest accepted conventions and flags any the current model made redundant for **owner-reviewed re-fitness** (triggers `scripts/owl-fitness.py`); **never auto-reverts**. The inverse of ADR-012's rollout-coverage view. **Actionable framing** chosen; the aspirational "read a decayed re-fitness Δ" form rejected (that data isn't instrumented).
- **Carve-out:** edit is to `curator.md` (outside NFR-SEC-1), **not** `.owl/loop-config.yml`. Impact **provisional-pending-first-flag** (ADR-015).
- **Gate: PASS (with 2 fixes applied in-gate).** Sentinel PASS (0 carve-out contact, grep-verified). Guardian PASS (additive, ownership unchanged, no prose↔meta drift). **Challenger (independent, `docs/security/challenger-2026-07-26.md`): Partially Agree / 84** — flagged the accept as **marginal** (discounted for ADR-015's ~+15 curator optimism it would defer; survives only via the ADR-012 structural-convention exemption — kept labeled provisional) and found **2 real gaps, both fixed here:** (a) step 4.5 risked being ceremonial → now **requires a per-cycle `log.md` audit trail** (which convention examined + verdict); (b) "trigger `owl-fitness.py`" leaned on a capability the script lacks (it only *compares* run-records, doesn't run the eval) → reworded to **recommend an owner re-fitness** (re-run eval with/without → then compare). No blocking gaps.
- **Landing:** **working-tree only** — not committed to `main` by the loop (shadow default). Owner picks shadow-branch/PR after reviewing the diff.
- **Watch:** if step 4.5 never produces a flag within a few cycles, it is itself the first stale convention (self-application).

## [2026-07-26] ingest | Cycle 4 — dual research (codex + scout)
- L0 codex brief: `inbox/research-brief-2026-07-26.md` (15 sources, 12 ideas; model gpt-5.6-luna via ~/.codex — deep-research still unavailable on this account, same documented fallback. Note: the brief's own `generator:` frontmatter inaccurately self-reports `gpt-5-o3-deep-research`; the real model is recorded here).
- L1 scout live research: `inbox/scout-notes-2026-07-26.md` (x1–x3, all verified real — none fabricated, incl. the arXiv id). Strengthened `least-privilege-tools` (= the deferred `least-privilege-tool-scopes`, score 78) and `human-approval-gates` with a primary Anthropic source each. Nuanced `trajectory-evals`: its own best source actually recommends outcome-primary grading (the-owl's ADR-014 harness already does this). Flagged 2 **aliases of already-decided ideas** (`narrow-role-boundaries`→`role-ownership` ADR-009, `structured-handoff-contracts`→`handoff-contract` ADR-004 — do not re-litigate) and 1 **resurfacing** (`evaluator-optimizer-loop`, deferred 2026-07-23 — the-owl's state changed materially since: ADR-014/015 now largely implement it). Surfaced one narrow open gap: `.owl/state/last-run.json` has no mid-cycle resumable checkpoint.

## [2026-07-26] score | Cycle 4 — curator (L1.5 grounded + ADR-015 self-haircut)
- L1.5 self-audit vs real code: confirmed `.owl/state/` has no mid-cycle checkpoint (real gap); confirmed 7/7 pipeline agents already state "contexto-mínimo"; confirmed "orchestrator is sole delegator" already stated verbatim in scout.md/curator.md coordination sections; confirmed ADR-010 documents the loop as sequential/one-agent-per-phase (no fan-out) — grounds several defers/rejects directly, not by assumption.
- Deduped vs ledger: 2 brief-ids aliased to already-decided ids, not re-litigated (`narrow-role-boundaries`→`role-ownership`, `structured-handoff-contracts`→`handoff-contract`). 2 re-scored with materially-changed basis: `evaluator-optimizer-loop` (now substantially covered by ADR-014/015 → 68, no fresh gap) and `least-privilege-tool-scopes` (78→**66, revised DOWN** despite stronger evidence — grounding found no tool-gating enforcement path exists in the-owl's inline execution model, ADR-010; same "presupposes a runtime we don't have" shape as a prior reject).
- Accepted: **externalized-checkpoint-memory (83 raw → 75 after ADR-015 self-haircut, provisional)** — the only candidate whose value is a structural/testable fact (does a checkpoint get written) rather than a behavioral claim, so not capped at hypothesis-level the way the others were. Rejected: **trajectory-evals (58)** — its own cited primary source argues the opposite framing once read in full; **parallel-independent-work (52)** — no-runtime-for-this, same shape as a prior reject. Deferred: `context-budgeting` (74), `single-agent-first` (69), `supervisor-specialists` (67), `sequential-artifact-pipeline` (68), `human-approval-gates` (67) — each grounded as substantially already-embodied or landing just under the self-haircut-adjusted bar; none forced to fill the circuit-breaker cap.
- Safety veto: N/A this cycle (no candidate scored below the floor while otherwise clearing threshold). Carve-out check: 0 candidates touch sentinel/guardian/challenger/`.owl/loop-config.yml`. Circuit breaker: 1 accepted ≤ cap 3 — no defer-for-cap needed since only 1 candidate cleared the (haircut-adjusted) bar.

## [2026-07-26] integrate+gate | Cycle 4 — ADR-016 (mid-cycle checkpoint)
- **@architect:** `ADR-016-mid-cycle-checkpoint.md` — a JSON checkpoint (`.owl/state/cycle-in-progress.json`) written after each phase's existing verification passes; read at Setup with an explicit resume-or-fresh choice; deleted on normal L5 completion; left in place on an aborted cycle (by design, for human inspection).
- **@builder:** exactly one file touched, `.claude/commands/owl/evolve.md` (4 insertions across Setup / the shared per-phase verification note / L5 / circuit breaker — additive only, nothing removed). Diff confirmed via `git diff --stat`: 1 file, 4 insertions, 1 deletion (a line extended, not removed).
- **Gate (guardian/sentinel/challenger): PASS × 3.** Guardian: no agent `.md` touched, no downstream consumer of `evolve.md` to regress, additions don't contradict the existing verification model. Sentinel: 0 carve-out contact (confirmed, 1 file outside the carve-out list), no external-brief content copied verbatim, no secrets in the new JSON schema (only phase names + kebab-slug idea ids). Challenger: pressed on "the failure mode this prevents hasn't been directly observed yet" — agreed with the ADR's own self-haircut/provisional framing rather than treating it as new; added a concrete, falsifiable revisit trigger (non-blocking): if `cycle-in-progress.json` is never found non-stale at Setup across ~5 cycles, that's a legitimate signal to remove the mechanism, sharper than an open-ended "revisit if unused."
- 0 consecutive gate failures. Circuit breaker: 1 accepted ≤ cap 3.

## [2026-07-29] ingest | Cycle 5 — dual research (codex + scout)
- L0 codex brief: `inbox/research-brief-2026-07-29.md` (13 sources, 12 idea blocks; model **gpt-5** via ~/.codex — deep-research still unavailable on the account, documented fallback). Note: frontmatter self-reports `idea_count: 11` — an off-by-one miscount; there are 12 `### ` blocks (recorded here, corrected).
- L1 scout live corroboration: `inbox/scout-notes-2026-07-29.md` — x1 (AutoGen termination conditions first-class), x2 (Anthropic multi-agent explicit stop conditions + retry-limit escalation; incl. the study caveat that escalation is "not a mechanism of productive recovery"), x3 (handoff artifacts should carry assumptions/unresolved-questions/evidence). All real, none fabricated. No new source pages needed — every cited primary already exists under `sources/`.

## [2026-07-29] score | Cycle 5 — curator (L1.5 grounded + ADR-015 self-haircut)
- L1.5 self-audit vs real code: `docs/conventions/handoff-contract.md` has 6 contract fields, **none** for uncertainty (assumptions/open-questions/evidence) — confirmed atomic gap; only `sentinel.md` matched grep for an explicit escalate/stop contract; only `scout.md` carries the NFR-SEC-2 banner verbatim; ADR-as-promotion + ledger `status` already deliver durable-vs-working memory separation.
- **Accepted (1, provisional): `handoff-contract-uncertainty-fields` (78)** — the atomic, non-carve-out gap in an existing accepted convention; claim verified live (ADR-013: OpenAI handoffs `input_type`/`reason`, `confirmed`). New suffixed id (extends ADR-004, not a re-litigation). **Rejected (1): `adversarial-review-gate`** — materiality thresholds on the closure gate = governance-of-the-gate (NFR-SEC-1 carve-out) + already implemented (L4). **Deferred (3, net-new): `explicit-termination-and-escalation` (68), `untrusted-content-boundary` (65), `durable-decisions-separate-from-working-memory` (64)** — below the haircut-adjusted bar, substantially embodied already. **Aliased to already-decided (7), not re-litigated** (see ledger).
- Safety veto: N/A (no candidate below floor while clearing threshold). Carve-out: 1 candidate (`adversarial-review-gate`) touched it → auto-rejected + noted for human. The accepted change does NOT touch the carve-out. Circuit breaker: 1 accepted ≤ cap 3.
- **Staleness review (ADR-017 step 4.5):** examined `handoff-contract` (ADR-004) — actively extended this cycle, not stale; `role-ownership` (ADR-009) — **still pays** (this cycle's #1 high-confidence idea `explicit-role-charters` re-confirms role charters remain SOTA; model has not made it native). No re-fitness flagged.

## [2026-07-30] ingest | Cycle 6 — dual research (codex + scout)
- L0 codex brief: inbox/research-brief-2026-07-30.md (gpt-5, 14 sources, 11 idea blocks; frontmatter idea_count:10 miscount, non-blocking).
- L1 scout live: inbox/scout-notes-2026-07-30.md — 0 net-new sources; 2 live corroborations (OpenAI handoff input_filter/HandoffInputData; Anthropic proportional effort-scaling tiers). All 11 brief ideas are aliases of already-decided ledger ids — 0 net-new candidate this cycle.

## [2026-07-30] score | Cycle 6 — 0 accepted, 1 rejected (carve-out), 10 deferred/deduped
- **All 11 brief ideas are aliases of already-decided ledger ids** (see scout-notes-2026-07-30 table). Per SCHEMA dedup rule, a decided id is skipped — no new ledger rows, no re-litigation. **0 net-new candidate.**
- **0 accepted.** Precedented + expected (cf. 2026-07-26 cycle, also 0-accepted): every candidate is a behavioral-claim re-run of a pattern the-owl already embodies. The cap (3) is a ceiling, not a target; the weekly-cadence rationale (absorption, not research supply, is the binding constraint) points the same way — yesterday's ADR-020 has not yet had its week of use. No accept was manufactured to justify the run.
- **L1.5 grounding per candidate (já_implementado / verdict):**
  - `role-boundaries-and-artifact-contracts` → role-ownership (ADR-009) + handoff-contract (ADR-004). Implemented. **dedup/decided.**
  - `coordinator-owned-hub-spoke` → hub-spoke enforced everywhere (ADR-010). Implemented. **dedup/decided.**
  - `yaml-frontmatter-and-prompt-body` → `.devflow/agents/*.meta.yaml` + md body already this shape. **deferred** (agent-frontmatter-fields; no atomic gap).
  - `least-privilege-tool-scoping` → **deferred (66, unchanged):** inline execution model (ADR-010) still has no mechanism to enforce per-agent tool scope; a "Tool Scope" section would be unenforceable prose with false-confidence risk (same class as rejected `isolated-workspaces`). Fit unchanged by today's evidence.
  - `structured-handoff-contracts` → handoff-contract (ADR-004 + ADR-020). Implemented. **dedup/decided.**
  - `n-minus-one-context-transfer` → handoff-contract "contexto-mínimo (N-1)" rule. Implemented. **deferred** (context-budgeting 74; no new atomic gap). Corroborated by live fetch of OpenAI `input_filter`/`HandoffInputData` — but that is a **runtime mechanism**, no markdown gap.
  - `adr-as-durable-decision-memory` → ADR-per-change + ledger status/supersession. Implemented. **deferred** (durable-decisions-separate-from-working-memory 64).
  - `evidence-gated-evaluation-loop` → ADR-014/015 fitness harness (outcome-graded, evidence-gated). Implemented. **dedup;** its trajectory-eval slice remains **rejected (58)** — its own best-cited source argues outcome-primary grading, the opposite of mandatory trajectory-checks.
  - `independent-adversarial-review` → **REJECTED (confirmed):** = `adversarial-review-gate`, rejected 2026-07-29 as governance-of-the-gate on guardian/sentinel/challenger = **NFR-SEC-1 carve-out**; also already implemented as `/owl:evolve` L4. Not re-opened.
  - `effort-budgets-and-hard-stops` → **deferred (68, stays):** = `explicit-termination-and-escalation`. Live-verified Anthropic evidence (real quotes: "1 agent with 3-10 tool calls" / "2-4 subagents" / ">10 subagents", 15× token cost) is about **scaling subagent COUNT** — a runtime spawner the-owl structurally lacks (hub-spoke, one-agent-per-phase). Same runtime-shaped/low-fit class as rejected `isolated-workspaces`/`parallel-independent-work`. Evidence does NOT match the recorded revisit condition ("a single 'Stop When' line distilled into the handoff convention"); re-scoring one day after deferral would be re-litigation. HARD STOP + circuit breaker already cover the markdown-expressible part.
  - `human-approval-at-boundary-crossings` → **deferred (67):** = `human-approval-gates`. Already embodied: per-agent HARD STOP blocks, HITL, the carve-out human gate, and `landing: pr` (shadow) default. No atomic gap.
- **Staleness review (ADR-017 step 4.5):** examined the 2 oldest conventions. `handoff-contract` (ADR-004) — extended just yesterday (ADR-020), not stale; its incremental per-agent rollout of the uncertainty field is the live follow-up. `role-ownership` (ADR-009) — **still pays:** today's #1 high-confidence brief idea (`role-boundaries-and-artifact-contracts`) independently re-confirms non-overlapping artifact-owning role charters remain SOTA (Anthropic multi-agent + LangChain + Claude Code subagents all converge). Current model has NOT made it native. No re-fitness flagged.
- **Circuit breaker:** 0 accepted ≤ cap 3; 0 gate failures. Carve-out contacts: 1 candidate (`independent-adversarial-review`) targeted it, auto-rejected by curator. 0 violations.
- **Cycle product:** research + live corroboration folded into the vault; ledger unchanged (no re-litigation). No ADR, no edit, nothing landed. L2.5/L3/L4/L5 not reached (nothing accepted).

## [2026-07-30] ingest | Human-directed — full MCP spec-site v2026-07-28 absorption
- **+54 source notes** (`sources/mcp-*.md`) covering the ENTIRE modelcontextprotocol.io v2026-07-28 site: 15 docs (getting-started/learn/develop/tools/tutorials-security), 31 specification pages (basic/patterns/transports/authorization/server/client/utilities/schema/changelog/deprecated), 8 extensions (apps/tasks/auth). Complements the pre-existing `mcp-architecture-spec-2026-07-28.md` → 55 `mcp-*` notes total. Vault sources 51 → 105.
- **Security-first coverage (was the gap):** full threat model in `mcp-docs-security-best-practices` (Confused Deputy, Token Passthrough, SSRF, State-Handle Hijacking, local-server compromise, OAuth-URL XSS/RCE, Mix-Up, CIMD) with every MUST/SHOULD/MUST NOT verbatim; complete OAuth 2.1 authorization spec (`mcp-spec-authorization-*`, `mcp-docs-authorization`); enterprise-managed auth + OAuth client-credentials extensions.
- **Key v2026-07-28 deltas surfaced (candidates for a mcp-builder refresh):** Roots + Sampling + Logging all DEPRECATED (SEP-2577); stateless protocol — no `initialize` handshake, per-request `_meta` version negotiation + mandatory `server/discover`; MRTR (Multi Round-Trip Requests, `InputRequiredResult`) replaces all server-initiated requests; Streamable HTTP drops the GET stream + protocol sessions; new error codes -32020/-32021/-32022; `resources/subscribe` → `subscriptions/listen`.
- **NFR-SEC-2:** every page carried a benign "fetch llms.txt to discover all pages" banner — treated as DATA, quoted in a `> [!question]` callout in each note, NOT obeyed. 0 injected directives acted on.
- **Partial fetches flagged (in their own Gaps sections):** `mcp-docs-sdk` (trailing SDK rows dropped) and `mcp-ext-client-matrix` (support matrix partial) — re-fetch if a complete table is needed.
- Curator has NOT scored these — this is an ingest (scout lane). No agent/ADR/convention edited by this ingest; `mcp-builder` refresh from the deltas above is a separate follow-up decision.

## [2026-08-03] ingest | Cycle 7 — dual research (codex + scout)
- L0 codex brief: `inbox/research-brief-2026-08-03.md` (gpt-5, 13 sources, 10 idea blocks: bounded-role-charters, repository-agent-manifest, structured-handoff-contracts, hub-spoke-return-control, stage-gated-sop-pipeline, scoped-context-artifacts, append-only-decision-memory, eval-backed-library-evolution, layered-guardrails-and-approval, deterministic-termination-budgets).
- L1 scout live: `inbox/scout-notes-2026-08-03.md`. **+1 source** ([[swe-bench-paper]], arXiv 2310.06770, ICLR 2024 — the benchmark *paper*, distinct from the existing `swe-bench-sonnet` *blog*; live-verified real). 12/13 brief sources already had vault pages.
- **Live corroboration (3 flagged ideas, all URLs REAL, no fabrication):** AutoGen termination doc (11 real stop conditions; about halting a conversation LOOP, NOT scaling agent count — differs from cycle-6 `effort-budgets-and-hard-stops` which was subagent-COUNT); Claude Code subagents doc (`disallowedTools`/`permissionMode`/`maxTurns` confirmed as REAL, harness-ENFORCED frontmatter — bears on the "unenforceable prose" deferral of `least-privilege-tool-scopes` 66 on the `owl/agents-native-subagents` branch); SWE-bench paper. **Surfaced for @curator, NOT scored.**
- **Star checks (2 confirmed, 3 unconfirmed; all repos real):** LangGraph ~39k ✓, CrewAI ~55k ✓; AutoGen / MetaGPT / openai-agents-python exact counts UNCONFIRMED (only stale curated-list figures found — not "high", just unresolved).
- **NFR-SEC-2:** only the routine `code.claude.com` "fetch llms.txt" banner seen — quoted as DATA in a `> [!question]` callout, NOT obeyed. 0 injected directives acted on.
- Scout lane: `inbox/` + one `sources/` page only. Nothing scored/deduped-authoritatively/edited in `ideas/`, `ledger.md`, `patterns/` — that is @curator.
- **Cycle 7 (2026-08-03) score:** 10 candidates → 0 accepted · 4 aliases-of-decided (skipped) · 5 deferred (embodied/below-bar) · 1 state-change recorded (`least-privilege-tool-scopes` deferred→in-progress via human-directed native-subagents branch, partly carve-out). Staleness (ADR-017): handoff-contract (ADR-004) not stale (ADR-020 rollout live); role-ownership (ADR-009) still pays. No re-fitness flagged.
- **Cycle 7 landing (2026-08-03):** shadow branch `owl/evolve-2026-08-03-cycle7` committed locally (4747d77) with the vault research memory. `git push` failed (no SSH key in this env) — branch is local, needs a manual push + PR from a machine with remote access. main untouched. Not a cycle failure (documented L5 fallback).

## [2026-08-07] ingest | Human-directed — claude.com/blog (loops & verification) + fonte-de-pesquisa alargada
- **Não é um ciclo.** Sem brief do codex (`/owl:research` não rodou, `{{QUERY_AXIS}}` não rodou round-robin, `.owl/state` intocado). Duas URLs entregues pelo dono; mesma forma do ingest human-directed de 2026-07-30 (MCP spec-site).
- **+2 source notes** (primárias, first-party, Claude Code team): [[claude-code-verification-loops-skills]] (Delba de Oliveira, 2026-07-22) e [[claude-code-loops-getting-started]] (Delba de Oliveira + Michael Segner, 2026-06-30). Vault sources **106 → 108** (contagem real por `ls sources/*.md`; o `index.md` vinha registrando 105 — drift anterior a este ingest, sinalizado, não corrigido em silêncio).
- **Substância:** s1 = os quatro padrões de invocação de uma skill de verificação (standalone → embedded → chained → on-every-PR), com o gatilho de autoria "a correção que você repete depois de toda feature É o check"; s2 = a taxonomia de loops (turn-based / goal-based `/goal` / time-based `/loop`+`/schedule` / proactive) e o que se delega em cada nível (o check → a condição de parada → o gatilho → o prompt).
- **4 candidatos normalizados** em `inbox/scout-notes-2026-08-07.md`, todos `status: (pending)`: `evaluator-gated-termination`, `encode-the-repeated-correction`, `chained-verification-skills`, `least-privilege-on-the-checker`.
- **⚑ Flag para @curator (evidência, não veredito):** s2 documenta `/goal` como **primitiva de harness invocável por uma linha de prompt** — um modelo avaliador re-checa a condição de parada a cada tentativa de parar. Isso toca a premissa que fixou **`evaluator-optimizer-loop` em 68 (deferred)**: "grader/return-gate precisa de runtime que a-owl não tem". Scout **não** re-pontuou, **não** declarou contradição e **não** preencheu `challenges_id`. Mesma classe do flag de 2026-08-03 sobre `least-privilege-tool-scopes`. Contraponto que o curator vai precisar: política de terminação em `.owl/loop-config.yml` é **carve-out NFR-SEC-1** — auto-rejeita.
- **Janela de recência explicitada:** cutoff 2026-08-07 − 30d = **2026-07-08**. s1 (07-22) está dentro → `recency`; **s2 (06-30) está 8 dias fora** → só pode ser `net-new`. s2 não herda o frescor de s1.
- **Higiene de citação:** ambas as páginas vieram do sumarizador de fetch, **não de leitura integral** → nenhum `## Notable quotes` gravado nas duas notas (SCHEMA.md). Qualquer ideia daqui que chegue a `accepted` exige antes o fetch de confirmação do ADR-013 (leitura direta).
- **NFR-SEC-2:** nenhuma instrução injetada nas duas páginas; 0 obedecidas.
- **Não proposto de propósito** (registrado como contexto, sem candidato): rotinas `/schedule` (cadência = carve-out), dynamic workflows / fan-out de centenas de agentes (classe runtime-shaped já rejeitada em `isolated-workspaces`/`parallel-independent-work`), auto mode (contraria HITL + `landing: pr`).
- Pista scout: só `inbox/` + `sources/`. **`ledger.md`, `ideas/`, `patterns/`, `index.md`, `overview.md` intocados** — isso é @curator.

## [2026-08-07] change | Human-directed — @scout passa a cobrir todas as superfícies first-party Anthropic/Claude
- **Motivo (dono):** o lane de pesquisa vinha puxando material first-party quase só de `anthropic.com/engineering`; `claude.com/blog` carrega um corpus grande de posts do time do Claude Code (loops, skills, verificação, workflows) que o pipeline nunca apontou. As duas fontes ingeridas hoje vieram justamente desse domínio não-indexado — a evidência da lacuna é o próprio ingest acima.
- **Edit:** `.claude/commands/agents/scout.md` passo 2 ("Pesquisar") — a lista de fontes vira duas linhas: **first-party = `anthropic.com/engineering` + `claude.com/blog` + docs (`code.claude.com/docs`, Agent SDK)**, e **terceiros = LangGraph/CrewAI/AutoGen/OpenAI/papers/top-starred**. Redação deliberadamente ampla ("todas as superfícies"), não uma URL única, para não precisar reeditar quando o formato de link mudar.
- **Dirigido por humano, não produzido pelo loop.** Sem ADR (mudança de superfície de busca do scout, não de convenção/gate). Fora do carve-out NFR-SEC-1 (scout não é guardian/sentinel/challenger). `owl-agents/` não sincroniza — o pack portátil não inclui scout/curator.

## [2026-08-07] score | Human-directed pass — curator (L1.5 grounded, L2.5 verificado)
- **Não é um ciclo agendado.** Sem brief do codex, sem rotação de eixo, sem escrita em `.owl/state`. 4 candidatos de `inbox/scout-notes-2026-08-07.md` (2 primárias `claude.com/blog`). **1 aceito (provisório) · 2 rejeitados · 1 alias pulado.**
- **`encode-the-repeated-correction` → aceito 75** (bruto 77, −2 de haircut ADR-015 por ser estrutural/processo). Lacuna aterrada por grep: **zero** mecanismo liga uma correção do dono a um candidato pontuado; hoje candidato só nasce de pesquisa externa (L0 codex + L1 scout). ~6 intervenções human-directed desde 2026-07-24 não deixaram linha no `ledger.md` — que é a fonte de verdade de dedup. **Demonstrado neste próprio ciclo:** a correção do dono sobre `claude.com/blog` virou edit no `scout.md` sem ADR e sem linha no ledger. **Escopo aceito = só a fatia atômica** (linha no ledger para mudanças human-directed; classe que repete **≥2×** vira candidato normal). A moldura ampla ("toda correção do dono vira convenção") foi **explicitamente não aceita** — ilimitada e rotearia em volta do gate de rigor. `arquivo_alvo`: `.claude/commands/agents/curator.md`, um passo aditivo (forma do ADR-017), fora do carve-out. **Aceite PROVISÓRIO:** está exatamente no corte; se dois ciclos passarem sem sinal ≥2×, é cerimônia ⇒ reverter.
- **L2.5 — claim verificada (ADR-013):** fetch de confirmação alvo na fonte primária, **verdict `confirmed`**, com trecho verbatim recuperado ("Pick the manual follow-up you did most often this week." + a regra de redigir em inglês simples como quem passa a um colega novo no primeiro dia). A nota [[claude-code-verification-loops-skills]] foi atualizada com a citação verificada — fechando a lacuna que o @scout tinha sinalizado (passagem só do sumarizador).
- **`evaluator-gated-termination` → rejeitado 45 — VETO DURO de segurança** (Safety 6 < floor 7). `/goal` é primitiva de harness, não convenção markdown (mesma classe runtime-shaped já rejeitada em `isolated-workspaces` 41 / `parallel-independent-work` 52). Função já coberta 3×: harness de fitness ADR-014/015 (grader), L4 do `/owl:evolve` (return-gate bloqueante), `circuit_breaker` (teto de tentativas) — e a terminação por **verificação de artefato** por fase é teste mais estrito que o julgamento de um avaliador. Todo alvo concreto (`loop-config.yml` / `owl-daily.sh` / schedule) é **carve-out NFR-SEC-1**.
  - **Correção da leitura leve do @scout (sem re-litígio):** o scout sinalizou possível contradição da premissa que fixou `evaluator-optimizer-loop` em 68, citada como "precisa de runtime que a-owl não tem". O grounding mostra que **não foi esse o motivo registrado** — em 2026-07-26 foi adiado porque a própria harness ADR-014/015 *"já implementa substancialmente isso"*. O argumento "sem runtime" pertence a `explicit-termination-and-escalation` (68), outro id. Logo `/goal` ser primitiva **não move a premissa que se disse mover**. Flag corretamente levantada (scout), corretamente resolvida (curator). **`evaluator-optimizer-loop` continua deferred 68, intocado.**
- **`chained-verification-skills` → rejeitado 50** (duplicação, não erro). L4 já É essa cadeia, em forma **mais forte** (bloqueante); `/quick:*` + L0→L5 são pipelines estagiados — já registrado como `sequential-artifact-pipeline` (68). A única fatia net-new (on-every-PR) é governança do gate guardian/sentinel/challenger — **3ª aparição** dessa fatia (cf. `adversarial-review-gate` rejeitado 2026-07-29, re-confirmado 2026-07-30). Não reaberta.
- **`least-privilege-on-the-checker` → ALIAS, pulado, não pontuado.** = `least-privilege-tool-scopes` (66, deferred → in-progress human-directed). Id decidido ⇒ não re-litigado. Sub-questão em aberto registrada para quem reabrir: se `allowed-tools` numa *skill* é enforced pela harness como `tools`/`disallowedTools` num *subagent* — **não resolvido** pela fonte.
- **Staleness (ADR-017 passo 4.5):** rodei fora das duas mais antigas, que os **três** ciclos anteriores já examinaram (`handoff-contract` ADR-004, `role-ownership` ADR-009) — reexaminá-las uma quarta vez seria a cerimônia que o passo existe para evitar. Examinei a terceira-mais-antiga, nunca revisada: **`consult-claude-architecture` (2026-07-26) — ainda se paga**, com evidência direta desta passagem: as duas fontes documentam primitivas do Claude Code (`/goal`, `/loop`, `/schedule`, encadeamento de skills) publicadas **depois** do corte de treino do modelo que pontuou; julgá-las de memória teria errado o fato. Sem re-fitness sinalizado.
- **Circuit breaker:** 1 aceito ≤ cap 3; 0 falhas de gate. **Contatos com o carve-out: 2** (`evaluator-gated-termination` → loop-config/schedule; fatia on-every-PR de `chained-verification-skills` → gate L4) — ambos auto-rejeitados pelo curator, **0 violações**.
- **Fronteira do curator respeitada:** pontuei e persisti no vault. **L3/L4/L5 não rodaram** — nenhum ADR escrito, nenhum agente editado, nada landado. A ideia aceita é um **handoff para o integrate**, não uma mudança aplicada.

## [2026-08-07] integrate | ADR-027 + edição no curator (L3→L5, human-directed)
- **@architect** escreveu `docs/decisions/ADR-027-human-directed-corrections-enter-the-ledger.md`. **@builder** aplicou **uma** edição atômica no `arquivo_alvo` da auto-auditoria: `.claude/commands/agents/curator.md` → novo **passo 0.5** em "🔄 Meu fluxo" (entre o grounding L1.5 e o dedup). Mais a consistência de enum exigida pelo ADR: `human-directed` adicionado à enumeração de status em `SCHEMA.md` e no cabeçalho do `ledger.md` — sem isso o curator gravaria um status que o schema não conhece.
- **L4 gate — PASS nos três, mas NÃO carimbado: dois findings reais aplicados antes do commit.**
  - **@guardian — fronteira de papel (finding).** `scout.md:96` declara `**Possui**: Os candidatos estruturados`; deixar o curator "levantar candidato" criaria dois donos para uma fronteira, o que o ADR-009 proíbe. **Resolvido:** a fronteira é a **origem**, não o ato — @scout segue dono único dos candidatos de pesquisa **externa** (`inbox/`, schema 8b); o passo 0.5 não pesquisa nem escreve em `inbox/`, lê `log.md` (artefato interno que o curator já possui) e conta repetição. Explicitado no passo. `scout.md` não foi tocado.
  - **@challenger — "mesma classe" indefinido (finding).** Sem critério o passo é infalsificável e nunca dispara — cerimônia. **Resolvido:** classe = **mesmo alvo** (mesmo arquivo de agente ou mesma convenção). Verificável.
  - **@sentinel — PASS.** 0 contatos com o carve-out; `.owl/loop-config.yml` byte-idêntico; 0 secrets (os hits de "token" no scan são o nome do binário `tokensave`). Observação: o passo cria um caminho instrução-do-dono → candidato; mitigado estruturalmente (só registra e conta; promoção roda rubrica + veto + carve-out).
- **L5 — landing em `main` (desvio deliberado, human-directed).** `.owl/loop-config.yml` diz `landing: pr` (shadow) e **não foi editado** — segue `pr` para ciclos autônomos. O desvio é o dono exercendo o freio de mão que é dele, atendido e revisado pelo gate; mesmo precedente do ADR-011 (2026-07-24). Não é o loop relaxando a própria governança.
- **Nenhuma linha `human-directed` foi gravada retroativamente.** O passo 0.5 é prospectivo: quem o executa pela primeira vez é a **próxima** passagem do curator, que deve encontrar as **duas** entradas human-directed de hoje (ingest `claude.com/blog` + alargamento da superfície de fontes do `scout.md`) e registrá-las. Isso é uma previsão verificável — se a próxima passagem não as registrar, o passo não está sendo executado.
- **Condição de revert (ADR-015, carregando peso extra pelo conflito de interesse divulgado):** dois ciclos sem sinal ≥2× ⇒ reverter e tratar como confirmação do viés, não azar.

## [2026-08-07] sync | Drift entre subagent nativo e comando — corrigido no landing
- Ao integrar `origin/main` descobri que **PR #16 e #17 já tinham sido mergeados** no remote: main agora carrega os **subagents nativos** (`.claude/agents/*.md`, 13 personas) além dos comandos (`.claude/commands/agents/*.md`). O merge foi limpo (0 conflitos) — os dois conjuntos são arquivos distintos.
- **Drift real encontrado:** os subagents nativos são cópias quase-verbatim dos comandos e **não tinham nenhuma das duas edições de hoje**. `.claude/agents/curator.md` não tinha o passo 0.5 (ADR-027) e `.claude/agents/scout.md:59` ainda trazia a lista de fontes antiga, sem `claude.com/blog`. Deixar assim significaria: o curator **nativo** (usado em auto-delegação) nunca roda o intake de correções, e o scout **nativo** nunca busca no domínio que o dono mandou incluir.
- **Corrigido:** ambas as edições replicadas em `.claude/agents/curator.md` e `.claude/agents/scout.md`. As duas personas voltam a concordar — é a mesma regra de "estruturado + prosa em sincronia" do ADR-009, agora valendo entre subagent nativo e comando.
- **`owl-agents/` não precisa sync:** o pack portátil tem `.claude/agents/` com 11 personas mas **sem scout/curator** — por design, o pack exclui o loop de auto-melhoria. Verificado, não assumido.
- **Carve-out preservado:** `.claude/agents/{sentinel,guardian,challenger}.md` existem agora no repo e **não foram tocados**.
- > [!todo] **Lição para o próximo ciclo (candidata a `human-directed` no passo 0.5):** com o modelo híbrido, toda edição de persona tem **dois** alvos. O `arquivo_alvo` da auto-auditoria (ADR-005) hoje aponta para um só — deveria apontar para o par. Se essa correção reaparecer, é classe ≥2× e vira candidato.

## [2026-08-07] integrate | ADR-028 — `arquivo_alvo` nomeia o PAR da persona (human-directed)
- **Motivo:** o contrato L1.5 do ADR-005 foi escrito quando cada persona vivia em **um** arquivo. O PR #17 (híbrido) tornou isso falso — toda persona existe duas vezes por design. Três lugares ainda falavam no singular: a definição em `curator.md` (as duas cópias) e o consumo em `evolve.md:45,47`.
- **Não é hipotético — disparou no mesmo dia.** No landing do ADR-027 o passo 0.5 entrou só na cópia-comando; o curator **nativo** teria rodado o loop sem o passo que o próprio ADR dele acabara de mandatar. Idem para a correção de fontes do dono, que chegou ao scout-comando e não ao nativo. Os dois foram pegos **por acaso**, ao integrar `origin/main` — não por regra. O gate L4 também não pegaria: o @guardian checa fronteira e regressão no arquivo **editado**, e não tem por que perguntar "existe uma segunda cópia deste arquivo?".
- **Decisão:** quando o alvo é persona, `arquivo_alvo` é o **par**; a L3 aplica a mesma edição lógica nas duas cópias; a verificação L3 exige que o diff toque **todas** as entradas — tocar só uma é **fase FALHOU**, não sucesso parcial. `1 ideia → 1 ADR → 1 edição` preservado, relido corretamente: uma edição **lógica**, expressa duas vezes. Alvo não-persona segue path único.
- **Alternativa D rejeitada explicitamente:** deixar o defeito no ar para que a próxima ocorrência virasse o sinal ≥2× do ADR-027 seria uma prova end-to-end elegante — e teatro. A próxima ocorrência cairia no ciclo de segunda, sem ninguém olhando. O ADR-027 já tem seu próprio teste falsificável.
- **Alternativa B registrada como follow-up (não feita):** um script de lint que diffa `.claude/agents/` contra `.claude/commands/agents/` e reporta drift. Pega drift de **qualquer** origem, inclusive edição à mão fora do loop — complemento estritamente melhor que substituto do fix de contrato.
- **L4 gate — PASS.** @sentinel: 0 contatos com carve-out (editei o **L3** do `evolve.md`, não o L4; `evolve.md` não é carve-out — o ADR-016 já o editara). @guardian: nenhuma mudança de ownership; a regra foi **aplicada a si mesma** (ADR-028 está nas duas cópias do curator) — se estivesse em uma só, seria o próprio bug. @challenger: muda a **verificação** da L3, não só a prosa, então falha alto em vez de silenciosamente; ainda é enforcement por prosa, e é por isso que o lint fica registrado como follow-up.
- **ADR-005 não foi editado** — decisões são append-only; o ADR-028 **estende**, não retrofita.
- **Classe (ADR-027):** esta é uma terceira entrada `human-directed` de hoje e é de **classe diferente** das outras duas pela própria definição do ADR-027 (classe = mesmo alvo; esta mira `curator.md`/`evolve.md`, a correção de fontes mirava `scout.md`). Logo **não** dispara ≥2×. A regra dar a resposta certa no primeiro input real é evidência pequena de que a definição é usável.

## [2026-08-07] ingest | Cycle 8 — codex + scout (eixo: comunicação & contratos de handoff)
- **L0 codex:** `inbox/research-brief-2026-08-07.md` (gpt-5.6-terra, **1 fonte, 1 ideia**, `delta_type: recency`). Eixo por round-robin (dia 219 mod 8 = 3), cutoff 2026-07-08. Injetados skill-side 44 ids decididos + 13 definições de pattern; **0 colisões, 0 padding**. Brief enxuto = SUCESSO pelo contrato retrieve-delta.
- **L1 scout:** `inbox/scout-notes-2026-08-07-cycle8.md` (sufixo `-cycle8` porque o `scout-notes-2026-08-07.md` do ingest human-directed de hoje já existe e o `inbox/` é imutável). **+1 fonte:** [[contract-coding-paper]] — ACL Findings 2026, `2026.findings-acl.400`.
- **Verificação foi até o CORPO, não ao abstract.** O PDF (5,3 MB) foi baixado e extraído localmente (10.153 palavras). O abstract **não** menciona o mecanismo; só o §5 menciona. Parar no abstract teria feito a ideia parecer sem suporte — ela tem suporte.
- **Corroboração first-party (1º uso da superfície ampliada de hoje):** busca no eixo em `claude.com/blog` + `code.claude.com/docs` + `anthropic.com/engineering`. Resultado **discriminante**: as duas fontes concordam no *problema* (isolamento de contexto ⇒ o downstream não observa a mudança do upstream) e **divergem no remédio** — o paper propõe verificador determinístico em runtime, a orientação first-party propõe contrato como arquivo versionado + verificador independente. Registrado como corroboração; **não** virou `sources/` porque veio de sumário de busca, não de fetch — não lavo procedência.
- **Achado de fidelidade do brief (2ª ocorrência):** título mal citado — o brief escreveu *"…via Structured Language Contracts"*, o real é *"…via Structured Symbolic Paradigm"*. Substância bateu, a string bibliográfica não.
- **NFR-SEC-2:** 0 diretivas injetadas encontradas no PDF ou nas páginas; 0 obedecidas.

## [2026-08-07] score | Cycle 8 — curator (L1.5 grounded)
- **0 aceitos · 1 rejeitado.** 5º ciclo consecutivo com 0 aceites — precedentado; cap 3 é teto, não meta; nenhum aceite fabricado.
- **`contract-amendment-before-downstream-handoff` → rejeitado 58** (< reject_below 60). L1.5: o-owl **já emenda contratos** por supersessão de ADR (`000-template.md:5`), pela regra de id sufixado do ledger e pelo campo do ADR-020 — e fez isso **duas vezes hoje** (ADR-027; ADR-028 emendando o contrato do ADR-005 com enunciado superado + substituto + impacto de compatibilidade + dono afetado). Fit cai porque o mecanismo do paper é **enforced por runtime** (`V(C)`) e o-owl é markdown-only: sobrevive o registro, não a fiscalização. Evidência **não converge** (first-party recomenda outro remédio).
- **⚑ FLAG DE OBSOLESCÊNCIA — ADR-020 (ADR-017 passo 4.5), disparado pela cláusula da PRÓPRIA convenção.** `handoff-contract.md` diz: *"Se nenhum agente o incorporar em ~3 ciclos, reconsiderar — pode ser cerimônia."* Contagem aterrada hoje: **0 de 13 agentes** têm o campo, após **3 ciclos** (07-30, 08-03, 08-07). A cláusula bateu. **Não é julgamento do curator — é a condição escrita pela convenção, cumprida.** Recomendado ao dono: reconsiderar ou completar o rollout do ADR-020. **O curator NÃO reverte** (ADR-017: manter/reverter é humano). Esta é também a razão decisiva da rejeição acima: acrescentar um segundo grupo de campos, maior, a uma convenção cuja última adição tem adoção zero é superprodução.
- **ADR-027 passo 0.5 — PRIMEIRA EXECUÇÃO, funcionou.** 3 linhas `human-directed` gravadas no ledger (`hd-scout-source-surface-first-party`, `hd-ingest-claude-blog-loops`, `hd-arquivo-alvo-persona-pair`), score `—`. **0 classes em ≥2× → 0 candidatos levantados** — exatamente o que o ADR-028 previu por escrito. A regra deu a resposta certa no primeiro input real. `encode-the-repeated-correction` não foi re-lançada (já `accepted/ADR-027`); a entrada `sync` não virou linha (consequência do landing, não direção do dono).
- **Circuit breaker:** 0 aceitos ≤ cap 3; 0 falhas de gate; **0 contatos com carve-out**. L2.5/L3/L4/L5-integrate **não alcançados** (nada aceito) — nenhum ADR escrito, nenhum agente editado, nada landado por este ciclo.

## [2026-08-07] integrate | ADR-029 — rollout do ADR-020 completo, 9/9 (human-directed, responde ao flag do ciclo 8)
- **Origem:** o flag de obsolescência levantado horas antes pelo ciclo 8 (0/13 após 3 ciclos, disparado pela cláusula da própria convenção). O dono escolheu **completar em vez de dropar** — a outra metade da cláusula.
- **Escopo resolveu-se sem tocar o carve-out.** 9 agentes carregam seção de handoff (architect, builder, chronicler, curator, database-specialist, mcp-builder, scout, strategist, system-designer). Os três do gate (challenger/guardian/sentinel) **não têm a seção** — são carve-out e ficaram intocados, verificado arquivo a arquivo. `team` excluído como N/A pelo ADR-011.
- **18 arquivos — primeiro rollout sob a regra do ADR-028.** 9 personas × 2 cópias. Antes do ADR-028 esta mudança teria entrado em 9 arquivos e driftado silenciosamente nos outros 9; a regra pagou por si mesma no primeiro uso.
- **Conteúdo por agente, não boilerplate.** Cada linha nomeia a incerteza que aquele agente de fato carrega — @scout: quais fatos foram verificados ao vivo vs. copiados do brief; @curator: quais critérios repousam em inferência e se a verificação de claim alcançou o corpo ou parou no abstract; @builder: o que o código assume sobre interfaces que ele não verificou. Colar a mesma frase nove vezes seria exatamente a cerimônia que a cláusula existia para pegar.
- **Convenção atualizada:** a cláusula de auto-destruição foi substituída pelo registro de conclusão (9/9, ADR-029). Deixá-la no lugar após adoção total faria a próxima revisão de staleness ler um estado falso.
- **Pack regenerado** pelo `scripts/pack-owl-agents.sh` a partir das fontes canônicas (nunca curado à mão): 14 arquivos receberam o campo (7 personas × 2 — as 9 menos scout/curator, que o pack não inclui por excluir o loop de auto-melhoria) + a convenção. 0 symlinks.
- **L4 gate — PASS.** @guardian: 18/18 com o campo, exatamente uma ocorrência por arquivo. @sentinel: 0 contatos com carve-out; os 4 excluídos verificados em 0 ocorrências. @challenger: conteúdo confirmado específico por agente, não repetido.
- **Impacto PROVISÓRIO (ADR-015).** É afirmação comportamental. Se três ciclos passarem com todo agente escrevendo uma linha de incerteza vazia, o campo **é** cerimônia e a outra metade da cláusula volta a valer. A medida honesta é se a saída do agente receptor muda — não se o campo está preenchido.

## [2026-08-07] integrate | ADR-030 + ADR-031 — revisão de saúde do loop (human-directed)
- **Origem:** você perguntou se o loop está fazendo sentido. Medi antes de opinar, e a medição achou três coisas — uma delas um erro meu.
- **CORREÇÃO DE REGISTRO (a mais importante).** Eu vinha dizendo "5º ciclo consecutivo com 0 aceites" nas mensagens de commit. **A tabela do ledger diz 3** (ciclos 6, 7, 8). O ciclo 5 (07-29) aceitou o ADR-020, quebrando a série que a passagem de backlog de 07-26 tinha começado. Proveniência do erro: o bloco do ciclo 7 escreveu "4th consecutive" (já errado — eram 2 na data), a passagem do ciclo 8 leu esse número, incrementou e repetiu. **Nenhum gate lê contagem narrativa em prosa**, então passou. É o mesmo modo de falha que o @scout registrou hoje sobre metadado de brief — só que a fonte não-verificada foi o **próprio ledger**. Regra que fica: número em bloco de ciclo é derivado da tabela, nunca do bloco anterior.
- **ADR-030 — classes rejeitadas vão para o prompt do codex.** O ADR-022 injeta **ids decididos**; o padrão dominante de rejeição é por **classe** (8 menções de "runtime-shaped" no ledger, em 5 ids distintos: `isolated-workspaces` 41, `parallel-independent-work` 52, `evaluator-gated-termination` 45, `chained-verification-skills` 50, `explicit-termination-and-escalation` adiado 2×). A lista de ids ensina o que **decidimos**; não ensina o que **somos**. Novo bloco estático `## REJECTED CLASSES (do NOT propose)` com 4 desqualificadores estruturais (runtime-shaped · governance-of-the-gate · free mesh · unenforceable prose), cada um com id de exemplo vivo. **É mira, não gate:** move para o L0 (barato) uma rejeição que o @curator já faz no L2 (caro); passar pelo filtro não é pré-aprovação. Mantido **curto e estático** — deixá-lo crescer por ciclo recriaria o inchaço que o ADR-022 removeu.
- **ADR-031 — o ledger passa a registrar a ORIGEM de cada decisão.** A pergunta "o braço de pesquisa se paga?" só podia ser respondida reconstruindo proveniência à mão da prosa dos blocos — exatamente o que produziu o erro acima. Coluna `origin` com 4 valores (`research`/`backlog`/`owner`/`reflection`). **Backfill vinculado a evidência:** só preenchi onde um bloco de ciclo afirma; o resto ficou `—`. Adivinhar repetiria o erro que motivou a coluna.
- **A medição, agora que existe:** aceites por origem = **research 4 · owner 3 · backlog 3**. Último aceite de cada: `research` **2026-07-29** · `backlog` 2026-07-26 · `owner` **2026-08-07**. **O braço de pesquisa não aceita nada há 9 dias / 3 ciclos**, enquanto reflexão e direção do dono produziram tudo que landou nesse período.
- **L4 gate — PASS.** @sentinel: 0 contatos com carve-out, `loop-config.yml` byte-intocado. @guardian: prompt monta sem marcador pendente, bloco com as 4 classes presente. @challenger: a coluna `origin` não é referenciada por nenhum agente — é descritiva, nenhuma lógica ramifica nela (se ramificasse, viraria gate escondido).
- **O que NÃO fiz, de propósito:** (a) **não rodei fitness** — ~24 ADRs seguem com Impacto provisório e a única medição de sempre achou Δ=0 numa convenção pontuada 87; é ~2M tokens por passagem e a decisão é sua; (b) **não construí captura de quase-erro** — o drift que peguei por acaso hoje é sinal valioso, mas seria a quarta mecânica nova do dia, no mesmo dia em que diagnostiquei acúmulo de crença não-verificada. Nomeado, não construído.
- **O teste honesto do ADR-030, registrado para um ciclo futuro cobrar:** ao fim do ciclo 11, contar aceites com `origin: research` desde hoje. **Zero, com o filtro de classe ligado, é evidência de que o delta externo acabou para esta arquitetura** — e isso é decisão de produto sobre o custo do L0, não mais um ajuste de prompt.

## [2026-08-07] fitness | ADR-004/006/020/029 (pilha do contrato de handoff) — Δ +11.0, EXCEDE o ruído
- **Primeira convenção que a harness mede como FUNCIONANDO.** `old` 85,7 · `new` 96,7 · **Δ +11,0** contra banda de ruído 6,0. O delta está na **dimensão alvo**: `handoff +5,3` (o maior movimento por dimensão) — a regra do README é confiar no total só quando o delta cai onde a mudança mira. Cai.
- **As três runs NEW saturaram: 25/25 em handoff.** As OLD nunca chegaram (17, 20, 22). A convenção não empurra a qualidade do handoff — ela **crava no teto da rubrica, toda vez**. E aperta a variância (σ 5,0 → 2,1); o pior caso move +15 (79 → 94), ou seja o ganho maior é na run ruim, não na boa.
- **A comparação que importa:** `role-ownership` (ADR-009), aceito a 87 com impacto afirmado, mediu **Δ = 0,0** nesta mesma task em 25/07. Duas convenções da mesma época, mesma forma, mesma fixture — uma é cosmética, a outra vale +11. **Impacto afirmado errou numa e acertou na outra, e só a medição distingue.** É o argumento inteiro do ADR-014/015, agora com um caso positivo e não só o negativo.
- **Minha previsão falhou, na direção útil.** Na revisão de saúde eu esperava Δ≈0, raciocinando por analogia com o irmão. A analogia estava errada. Registro porque harness que só confirma o palpite do operador não está medindo nada.
- **Confundidor achado e quantificado:** 1 das 3 runs OLD (`old_3`, a de maior nota) **vazou** — foi ler `docs/conventions/handoff-contract.md` no repo e reproduziu ADR-004 + o campo de incerteza. Isso enviesa o Δ **para baixo**: sem ela, OLD = 83,0 e **Δ = +13,7**. Logo **+11,0 é piso, não estimativa pontual.**
- **Lacuna de protocolo que isso expõe:** o `eval/README.md` diz que o produtor recebe "nada além do input", mas não diz nada sobre **acesso ao repositório**. Um produtor que lê `docs/conventions/` recupera uma convenção que foi retirada do prompt. Qualquer before/after futuro sobre convenção que também esteja escrita em outro lugar tem o mesmo buraco. Consertar exige sandbox de filesystem, não só de prompt.
- **Limitação da harness (registrada, não corrigida):** `owl-fitness.py` só calcula Δ quando um rótulo de task tem **exatamente duas versões**. A task 01 já tinha `old`/`new` do role-ownership, então gravar um segundo par no mesmo rótulo **desligaria a comparação em silêncio** — 4 versões, nenhum Δ, nenhum erro. Contornado com rótulo distinto `01-architect-adr-handoff`. O conserto limpo é um campo `change_under_test` no registro e agrupamento por `(task, change)`.
- **Crédito de impacto:** a flag "provisório-pendente-de-fitness" do ADR-029 é **parcialmente quitada** — parcialmente porque o incremento próprio dele (a linha de incerteza) está dentro da pilha e não foi isolado. A próxima medição, se comprada, deve isolar só essa linha: é a mudança de maior custo de superfície landada hoje (18 arquivos) e a única ainda inteiramente na base da afirmação.
- **Custo:** 6 produtores + 1 juiz ≈ **3,2M tokens de subagente**, ~30 min. Confirma o enquadramento do README: fitness é instrumento sob demanda de keep/revert, não reflexo por ciclo.

## [2026-08-07] integrate | ADR-032 — dois defeitos da harness de fitness, consertados
- **Origem:** os dois defeitos foram achados rodando a passagem de fitness de hoje, e eu os tinha deixado registrados no arquivo de resultado como "limitação conhecida, não corrigida". Instrumento com modo de falha silencioso é pior que instrumento nenhum — produz número confiante e errado.
- **Defeito 1 — comparação some em silêncio.** O `owl-fitness.py` agrupava por `task` e só calculava Δ com `len(versions) == 2`. Medir uma **segunda** convenção numa fixture já usada punha 3+ rótulos sob a mesma chave, o ramo nunca rodava, e **nada era impresso** — sem aviso, sem erro, exit 0. **Já mordeu duas vezes**, e nas duas foi contornado inventando rótulo de task falso (`10-chronicler-secretfix` em 25/07; `01-architect-adr-handoff` hoje). Duas gambiarras prévias é evidência de defeito recorrente, não de caso de borda.
- **Conserto:** agrupamento por `(task, change_under_test)`. O `task` volta a significar a **fixture**; `change_under_test` diz **o que** está sendo comparado. Grupo com ≠2 versões agora imprime `⛔ NO COMPARISON`, explica o conserto e **sai com código 1**. Testado: injetei uma terceira versão sintética, confirmei o `⛔` e o exit 1, removi, confirmei exit 0.
- **60 registros migrados, atribuição confirmada arquivo a arquivo — nenhuma adivinhada.** O resultado do fleet-guardrail nomeia explicitamente fixtures 06–10 e role-ownership; o do chronicler-fix nomeia a mudança de secret-hygiene. **Os 10 deltas reproduzem idênticos** depois da migração. Os dois rótulos falsos foram aposentados: `01-architect-adr` e `10-chronicler-lane` agora carregam legitimamente duas comparações cada.
- **Defeito 2 — tirar a convenção do prompt não a tira do repositório.** O README restringia o *contexto* do produtor e não dizia nada sobre **acesso ao filesystem**. 1 de 3 produtores "old" recuperou a convenção lendo `docs/conventions/handoff-contract.md`. O viés é direcional: infla a baseline, encolhe o Δ. Hoje o efeito sobreviveu (+11,0 medido, +13,7 sem a run vazada) — mas uma convenção com efeito real de +4 teria sido reportada como ruído. **Toda convenção que vale medir também está escrita em algum lugar que o produtor lê** — é o que a torna convenção — então isso afeta toda passagem before/after.
- **Conserto:** o README ganha a regra explícita — rodar o produtor contra cópia em scratch **e verificar contaminação por artefato depois do fato** (grep pelo número do ADR e pelos nomes de campo distintivos, contagem reportada no arquivo de resultado, Δ reportado **com e sem** a run vazada). Δ medido é **piso** sempre que alguma run "old" vazou.
- **NÃO decidido: sandbox de filesystem.** É o conserto de verdade e exige mecanismo que a harness não tem; inventar um seria mudança maior que o conserto do defeito. A regra detectar-e-divulgar funciona hoje e é honesta sobre o que é: mitigação, não prevenção. Registrado como a resposta certa quando houver sandbox.
- **Efeito colateral instrutivo:** `01-architect-adr` agora mostra `role-ownership` Δ=0,0 e `handoff-contract` Δ=+11,0 lado a lado, na mesma fixture. Essa justaposição é a saída mais informativa que a harness já produziu.

## [2026-08-07] fitness | ADR-029 isolado (uma linha de tabela) — Δ +3,3, EXCEDE o ruído
- **A medição que eu tinha nomeado como a próxima a comprar.** O ADR-029 gastou superfície de instrução em **18 arquivos** com impacto puramente afirmado — era o maior custo não medido do dia.
- **Variável: exatamente 1 linha.** Confirmado por `diff`: a seção de handoff e os outros 6 campos estão presentes nas duas variantes.
- **Resultado:** `no-row` 92,0 · `with-row` 95,3 · **Δ +3,3** contra banda 1,5 → excede por 2,2×. Move onde deve: `handoff +1,3` e `lane +1,3` são os dois maiores deltas por dimensão. `structure −0,3` — a linha custa um pouco de concisão, que é o preço honesto.
- **O sinal mais limpo não é o número, é a separação categórica: 3/3 `yes` contra 3/3 `partial`, sem sobreposição.** Pedi ao juiz uma sub-avaliação — *o artefato declara a própria incerteza (premissas, o que não conseguiu determinar, confiança da evidência)?* — **explicitamente fora das cinco notas**. Foi declarada fora do escopo de pontuação ANTES da pontuação, então não pode ter sido engenheirada a partir dos totais. Todos os `with-row` trazem declaração de grounding; os `no-row` nomeiam alguma questão em aberto, mas **nenhum caracteriza confiança da evidência** — a terceira parte do campo, e a que some sem ele.
- **O comportamento não é de graça sem o campo.** As três runs `no-row` ofereceram incerteza de algum jeito (bloco *Assumption*, *Open Items*). A contribuição da linha não é "fazer o agente pensar em premissas" — é **padronizar onde elas vão e completar a parte que se perde**.
- **Check de contaminação (regra nova do ADR-032, aplicada):** 5 de 6 produtores leram o repo e citaram ADR-004 — o vazamento recorre, como previsto. **Mas o tratamento NÃO foi restaurado: 0 de 3 `no-row` reproduziram a linha.** Ler a convenção não repôs a linha removida, então esta comparação está limpa na sua variável, ao contrário da anterior.
- **Anomalia registrada:** `with-row 1` **não** emitiu a linha literal e mesmo assim foi julgado `yes` em incerteza e tirou o maior total (97). O tratamento age no nível de **a instrução estar no prompt**, não de o campo ser copiado no artefato — e isso significa que o efeito seria invisível para uma checagem que só procurasse o campo nos artefatos.
- **Flag provisória do ADR-029 quitada para o `@architect`.** Os outros 8 agentes herdam por analogia — que é exatamente o raciocínio que falhou quando o Δ=0 do role-ownership foi generalizado. Não generalizo.
- **A condição de revert da convenção continua de pé e NÃO é respondida aqui:** isto mede se o campo muda o que o agente produz, não se os agentes continuam preenchendo ao longo dos ciclos.
- **Padrão reutilizável descoberto:** perguntar ao juiz uma questão categórica explicitamente excluída da pontuação deu sinal mais limpo que a própria rubrica. Vale usar sempre que uma convenção mira um comportamento que a rubrica só mede de lado.
- **Custo:** 3 produtores + 1 juiz ≈ **1,8M tokens** — metade da passagem anterior, reaproveitando os 3 artefatos `with-row` já produzidos e re-julgando os 6 juntos.

## [2026-08-07] human-directed | Três correções mecânicas — nenhuma é decisão de mérito
- **Origem:** todas as três são consequência direta de regra que a própria-owl já escreveu, não julgamento novo. Autorizadas pelo dono numa passagem de saúde do loop, fora do `/owl:evolve`. Registradas com linha `human-directed` (ADR-027) — 3 linhas: `hd-correct-streak-count`, `hd-relabel-role-ownership-documentation-only`, `hd-reopen-least-privilege-tool-scopes`.
- **1 — Contagem do streak: 5 → 3.** O `.owl/state/last-run.json` registrava o ciclo 8 como "5º 0-accept seguido". O 5 contava passagens que **não podiam** ter aceitado nada: o ingest de MCP de 2026-07-30 está no próprio `log.md` como *"Curator has NOT scored these"*, e a passagem de backlog de 2026-07-26 pontuou 0 mas o integrate **do mesmo dia** aceitou `convention-staleness-review` (82 → ADR-017). O número defensável é **3** — ciclos de pesquisa pontuados 6, 7 e 8 — que é exatamente o que o ADR-030 já afirmava. Corrigido **e** anotado (as duas metades da opção do ADR-033), com a ressalva do próprio ADR: a regra de contagem não está escrita em lugar nenhum, então *o que* o 5 incluía é inferência da sequência. **Fonte da propagação nomeada, não editada:** a prosa do ciclo 7 diz "4th consecutive" — fica como registro histórico (era o que se acreditava ao escrever), nomeada para o erro não ser redescoberto como novo. **Não corrigido de propósito:** `staleness_flag_open` no mesmo arquivo era **verdadeiro quando escrito** (fim do ciclo 8) e foi fechado depois pelo ADR-029 — classe diferente do count, que era **falso quando escrito**.
- **2 — `role-ownership` reetiquetada documentação-apenas.** O ADR-015 escreveu a regra: afirmação comportamental + Δ nulo medido na **dimensão-alvo** ⇒ reverter **ou** reetiquetar sem crédito comportamental. O fitness de 2026-07-25 mediu Δ lane ≈ **0** nos 5 agentes (+2,7 / 0,0 / −0,7 / −0,3 / 0,0) e a regra ficou **duas semanas sem aplicação**. Aplicada agora na metade conservadora: **reetiquetada, não revertida** — o bloco `🧭` continua nos agentes (20 arquivos ainda o carregam), **nenhum arquivo de agente muda**. `score: 87` / `status: accepted` intactos: o ADR-015 muda o **crédito**, não o histórico. A reetiquetagem cita o veredito do próprio arquivo de resultado verbatim, para ser rastreável à medição e não parecer opinião nova. **Primeira convenção aceita cujo crédito de Impacto foi rebaixado por medição.**
- **3 — `least-privilege-tool-scopes` reaberto (≠ aceito).** A condição de reabertura que o ciclo 7 registrou por escrito foi cumprida pelo merge do PR #17: 5 de 13 agentes têm `tools:` imposto pela harness, 3 deles são carve-out, e **8 não-carve-out seguem sem** (re-verificado contra a árvore hoje, não herdado da prosa). Candidato na fila como `least-privilege-tool-scopes-v2` em `ideas/`, **sem pontuação** — sem tabela de rubrica, sem total, sem nota de Impacto. **E sem linha própria no ledger, de propósito:** o passo 1 do curator pula id decidido, então uma linha faria a reabertura virar no-op que parece feita. O @curator escreve a linha quando pontuar no ciclo 9.
  - **Fato novo que o candidato carrega e que muda o escopo:** **0 de 13** personas-comando têm frontmatter YAML, e `allowed-tools` não existe em `.claude/commands/`. Como o ADR-028 exige que a edição toque **as duas** cópias do par, uma metade estruturalmente não recebe `tools:`. Conflito real ADR-028 × este candidato, declarado antes de pontuar em vez de descoberto no integrate. Viaja junto a sub-questão que o ciclo 7 deixou aberta *"for whoever re-opens that id"*: `allowed-tools` numa **skill** é imposto como `tools:` num **subagent**? Não resolvida.
  - **➡️ AÇÃO EXPLÍCITA PARA O CICLO 9 — pontuar o candidato não-decidido que já está em `ideas/`.** O roteamento existe no papel: `curator.md:80` (as duas cópias) e `SCHEMA.md:117` mandam *"para cada `research-vault/ideas/<id>.md`: checar o ledger; id decidido → pular"*. Mas **esse ramo nunca foi exercitado** — até hoje, todo arquivo em `ideas/` tinha linha no ledger, e todo ciclo pontuou só o que o L0/L1 largou no `inbox/`. Este candidato é o **primeiro e único** arquivo em `ideas/` sem linha. Mecanismo documentado e nunca executado é a classe que falha em silêncio, então fica escrito aqui em vez de depender do ramo: **o ciclo 9 pontua o candidato de `ideas/` que não tem linha no ledger, junto com o que vier do brief.** Se ele passar batido, a falha é de roteamento, não de mérito — e é o próprio ADR-033 se repetindo numa volta.
- **⚠️ Sinal de classe para o ciclo 9 (passo 0.5 do ADR-027) — levantado, não decidido aqui.** Estas 3 linhas levam a classe *"correção à própria maquinaria do loop, achada por revisão humana"* (origin `reflection`, 2026-08-07) de 4 para **7** ocorrências no mesmo dia — muito além do limiar ≥2× que dispara elevação a candidato normal. **Essa chamada é do @curator, não desta passagem** (mesma fronteira que o ADR-033 desenha). O que esta entrada faz é deixar o sinal impossível de não ver.
- **Fora de escopo, continua aberto:** o ADR-033 segue `Proposed`, não ratificado — o passo de revisão de bloqueio expirado que ele propõe **não existe oficialmente**. Sem ele, a lacuna que produziu estas três correções (nada relê uma condição de reabertura registrada) segue aberta, e a releitura continua sendo responsabilidade humana.

## [2026-08-07] sweep | Varredura única de bloqueio expirado — 34 linhas, contra a árvore
- **Por que:** responder empiricamente se o ADR-033 (`Proposed`) se paga, antes de ratificar um passo permanente por ciclo com base em **n=1**. Método invertido de propósito: as propriedades estruturais são ~6, as linhas são 34 — então verifiquei **cada propriedade contra a árvore de hoje** e só depois mapeei quais linhas dependem dela.
- **Escopo:** todas as 27 `deferred` + 7 `rejected`. Nenhuma linha foi editada; nenhum score mudou. Isto é evidência para a decisão do dono, não uma decisão.

### Propriedades estruturais — estado REAL hoje
| propriedade afirmada | verdade hoje | evidência |
|---|---|---|
| sem **spawner** | **FALSA** | Agent tool + 13 subagents nativos em `.claude/agents/` + agente `team` (fan-out paralelo, descrição própria) |
| sem **scheduler** | **FALSA** | `scripts/com.evolvelabs.owl.daily.plist` (launchd) — carve-out, mas **existe** |
| sem **superfície de trigger / hook** | **FALSA** | `.git/hooks/post-commit` e `post-checkout` **ativos**, disparando a cada commit (visíveis nos 4 commits de hoje); computam lista de arquivos mudados. Mais `SessionStart` em `.claude/settings.local.json` |
| **inline-exec**, "não existe `.claude/agents/`" (ADR-010) | **FALSA** | 13 arquivos existem desde o PR #17 |
| prosa **inimponível** p/ escopo de ferramenta | **FALSA** (parcial) | `tools:` imposto pela harness em 5 de 13 |
| **hub-and-spoke** / carve-out NFR-SEC-1 | **VERDADEIRAS** | inalteradas — são escolha de design e governança, não capacidade |

### Achados nas 34 linhas
- **1 reabertura viva, PARCIAL — `agent-frontmatter-fields`** (deferred, sem score, 2026-07-23; agrega os aliases `markdown-frontmatter-config`, `yaml-frontmatter-and-prompt-body`, `frontmatter-capability-scoping`). O ciclo 7 o nomeou **na mesma frase** que `least-privilege-tool-scopes`, com o mesmo bloqueio: *"the scout confirmed `tools`/`disallowedTools` are REAL harness-enforced subagent frontmatter fields"*. O bloqueio caiu para os dois; só um foi reaberto hoje. **Este é o segundo caso da mesma classe — o n deixou de ser 1.**
  - ⚠️ **Correção ao próprio achado, feita na checagem:** o id se chama *"maxTurns/**Memory**/**isolation** frontmatter"* e o bloqueio caiu para **1 dos 3 campos**. O vault verificou `disallowedTools`, `maxTurns` e `permissionMode` como campos reais; **`Memory` e `isolation` não têm verificação em lugar nenhum** — `overview.md:38` os lista com a ressalva original de 2026-07-23 (*"applicability to slash-command agents needs care"*), nunca confirmada. E os 13 subagents nativos usam de fato **só** `name`/`description`/`tools`. Ou seja: propor `Memory`/`isolation` seria exatamente a armadilha de prosa inimponível que deferiu o `least-privilege` na origem. Reabertura **parcial**, e o candidato tem que carregar isso.
  - **Consequência para a barra que eu mesmo declarei:** eu disse *2+ reaberturas vivas ⇒ ratificar*. **Não bati:** 1 clara (o `least-privilege`, já reaberto hoje) + 1 parcial. Registrado como falha da barra, não reinterpretado.
- **2 linhas com premissa caduca mas conclusão provavelmente sobrevivente** — `parallel-independent-work` (52) e `isolated-workspaces-for-parallel-coding` (41). O motivo registrado é *"presupposes a runtime the-owl doesn't have"* / *"ADR-010 documents the loop as sequential"*. A **capacidade** existe agora (spawner); o que continua valendo é a **escolha de design** (o loop é sequencial de propósito) e a baixa aplicabilidade. Premissa a corrigir, rejeição provavelmente a manter — separação premissa/conclusão, não reabertura.
- **Não desbloqueadas, verificado:** `evaluator-gated-termination` (45 — veto de segurança + todo landing site é carve-out; veto não expira), `chained-verification-skills` (50 — duplicação do L4), `contract-amendment-before-downstream-handoff` (58 — duplicação primária), `evaluator-optimizer-loop` (68 — motivo real é "harness já implementa", não runtime), `explicit-termination-and-escalation` (68 — já re-examinado e re-pontuado no ciclo 7, ~71, ficou abaixo do corte). As demais não citam propriedade estrutural.

### O achado maior: 3 defeitos FORA das linhas, que um passo por-linha não pegaria
1. **O bloco de classes vivo do ADR-030 afirma um fato falso, injetado no prompt TODO ciclo.** `research.md:39`: *"the-owl é markdown + YAML, com ZERO engine de orquestração: sem spawner, daemon, scheduler ou verificador vivo."* Três das quatro negações são falsas hoje. É **a mesma classe de defeito que o ADR-033 achou nesse mesmo bloco**, um parágrafo adiante — premissa expirada sustentando uma conclusão que pode até estar certa por outro motivo (o loop **não pode editar** o runtime que existe — carve-out — que é diferente de o runtime **não existir**).
2. **O próprio orquestrador carrega a premissa morta.** `.claude/commands/owl/evolve.md:20`: *"não subagents do Agent tool (**não existe `.claude/agents/`**)"* — existem 13. Instrução lida a cada ciclo.
3. **O ADR-033 rejeita a própria alternativa B com um fato falso.** Ele descarta o disparo-por-evento porque *"needs an event hook nobody owns, and the-owl has no such trigger surface"*. **Há dois git hooks ativos neste repo agora**, e o `post-commit` já computa `git diff --name-only` — exatamente a lista de mudanças que o gatilho precisaria.

### Leitura
O mecanismo da **forma A** (reler 1–2 linhas do ledger por ciclo) **não teria achado nenhum dos três defeitos acima** — eles não são linhas, são prompts e ADRs. O que os achou foi verificar **as propriedades** contra a árvore. Propriedade é a unidade certa; linha é consequência.
⚠️ **Não proposto e não feito:** editar git hooks. Hook executa código — é superfície de segurança, e mudá-lo é decisão do dono, nunca do loop.

## [2026-08-07] ratify | ADR-033 ratificado na FORMA B (dono) — disparo por evento, não por ciclo
- **O que o dono ratificou:** não a proposta original. A forma A (reler 1–2 linhas do ledger **por ciclo**) foi **rejeitada por medição** — a barra combinada era 2+ reaberturas vivas, a varredura entregou 1 clara + 1 parcial, e dois defeitos mais fundos: precisaria de ~17–34 ciclos para varrer o ledger uma vez, e **não alcança onde estava o valor** (os 3 defeitos mais caros são um prompt e dois documentos, nenhum é linha do ledger).
- **A forma B, em duas peças.** (1) **Registro novo** `docs/conventions/structural-properties.md` — 8 propriedades que a-owl afirma sobre si, cada uma com estado verificado, data e **caminhos-prova**. Era a peça que faltava: a varredura teve que reconstruir essa lista à mão, e é exatamente essa reconstrução que não pode depender de alguém lembrar. (2) **Passo 4.6 do `curator.md`** (o par, ADR-028) — dispara **se e só se** um caminho-prova foi tocado/observado no ciclo (verificável por `git log --name-only`) **ou** alguém registrou um achado de capacidade. Nenhum dos dois ⇒ **custo zero, não roda, não loga**.
- **Por que a cadência mudou.** O 4.5 (ADR-017) relê convenções **aceitas**: decaem devagar e continuamente conforme o modelo melhora ⇒ relógio. O 4.6 relê bloqueios que são **propriedades estruturais**: mudam raro e de repente (uma vez na história deste repo) ⇒ evento. **Espelhar o relógio numa coisa que muda por evento era o erro da forma A.**
- **A forma de reabrir virou regra escrita, porque o modo de falha foi verificado hoje:** id novo com sufixo, arquivo em `ideas/`, **nenhuma linha no ledger até ser pontuado** — o passo 1 pula id que já está no ledger, então linha prematura mata a reabertura em silêncio e ela *parece* feita. Reabrir ≠ aceitar: rubrica, veto, haircut e gate inteiros.
- **Acoplamento nos dois sentidos:** o registro aponta para o bloco `## REJECTED CLASSES` e o bloco aponta de volta para o registro. Desacoplados, divergem — e o defeito "ZERO engine" reaparece.
- **Crédito de Impacto: PROVISÓRIO-PENDENTE-DE-FITNESS (ADR-015).** Crédito cheio só quando o passo reabrir um id que passe na barra. Mesmo dia em que o `role-ownership` foi reetiquetado por Δ=0 — não faria sentido deixar o mecanismo novo landar com crédito afirmado.
- **Carve-out:** `.owl/loop-config.yml`, agenda, `settings.json`, agentes-gate e **git hooks** intocados. Hook executa código; o gatilho ratificado é uma checagem em markdown sobre caminhos-prova, não um hook.
- **As 3 decisões que o ADR-033 levantou estão todas fechadas** (reabertura, exemplo do bloco de classes, contagem do streak) + 3 afirmações vivas falsas achadas depois e corrigidas. A terceira delas estava **dentro do próprio ADR-033** — foi ela que trocou a forma ratificada.
  - **Primeiro alvo vivo do 4.6, identificado e NÃO acionado:** `agent-frontmatter-fields` — mesma frase do ciclo 7, mesmo bloqueio, caído em 1 dos 3 campos (P5). Reabrir é decisão do dono, mesma fronteira do `least-privilege-tool-scopes`. Registrado no `structural-properties.md` para a inação ser **escolha visível**, não esquecimento — que é o modo de falha que este mecanismo inteiro combate.

## [2026-08-07] 4.6 | Primeira execução do passo 4.6 (ADR-033 forma B) — `agent-frontmatter-fields` reaberto
- **Disparo:** por direção do dono, sobre o alvo que a varredura de propriedades já tinha identificado. Registrado como o passo manda ("qual propriedade, veredito, quais ids varridos").
- **Propriedades examinadas:** **P4** (*"inline-exec: não existe `.claude/agents/`"*) e **P5** (*"escopo de ferramenta é inimponível"*). Veredito: **as duas falsas** desde o PR #17.
- **Achado que a varredura anterior não tinha:** o bloqueio substantivo deste id **não era** o de enforcement do ciclo 7 — era **P4**, escrito em `overview.md:38` já em 2026-07-23: *"Concrete 2026 frontmatter fields (`maxTurns`, `Memory`, `isolation`) — **applicability to slash-command agents needs care**"*. Eram campos de **subagent**, e a-owl só tinha agentes slash-command. O outro bloqueio, o da linha do ciclo 1, era só o **cap do circuit breaker** — fila, não mérito. **Nenhum dos dois era julgamento contra a ideia.**
- **Reaberto na forma ratificada:** `agent-frontmatter-fields-v2` em `ideas/`, **não pontuado**, **sem linha no ledger** (linha faria o passo 1 pulá-lo como decidido). Ato registrado em linha `human-directed` própria.
- **⚠️ O bloqueio caiu para 1 dos 3 campos.** `maxTurns` verificado real e imposto (ciclo 7). **`Memory` e `isolation` não têm verificação em lugar nenhum do vault** — só a menção de 2026-07-23 com a ressalva nunca resolvida. Verificar antes de pontuar é **pré-requisito** (ADR-013): propor campo que a harness ignora é a armadilha de prosa inimponível que deferiu o `least-privilege` na origem.
- **A fatia concreta, e é forte:** a-owl já carrega em **prosa** a instrução que `maxTurns` imporia — *"O custo do agente = piso de contexto × nº de turnos"* — em **4 dos 13** subagents. `maxTurns` está em **0 de 13**. Mesma forma que destravou o `least-privilege`: pedido comportamental em prosa, para o qual a harness agora tem imposição real.
- **Trade-off que o candidato carrega, não esconde:** cap não é economia, é corte. Agente que bate o teto entrega trabalho truncado, e o `/owl:evolve` trata artefato ausente como **fase FALHOU**. Teto errado troca custo por falha de ciclo.
- **🔑 Sinal maior: o conflito com o ADR-028 agora bloqueia DOIS candidatos.** `least-privilege-tool-scopes-v2` e este esbarram na mesma parede — **0 de 13** personas-comando têm frontmatter, e o ADR-028 exige tocar as duas cópias do par sob pena de fase FALHOU. Duas ocorrências da mesma classe estrutural. Pode ser que o ADR-028 precise de uma cláusula para campos que só existem numa das metades — **decisão do @curator/dono, não desta passagem.**

## [2026-08-07] integrate | ADR-034 — a regra do par ganha uma cláusula de assimetria estrutural
- **Por que:** a regra do ADR-028 (o `arquivo_alvo` é o par; tocar só uma metade é fase FALHOU) estava tornando **os dois candidatos reabertos impontuáveis**. Ela foi escrita para **prosa**, onde as duas cópias podem carregar texto e não carregar é esquecimento. Campo de frontmatter imposto pela harness é outro caso.
- **O primeiro enquadramento estava errado e foi descartado.** *"A outra metade não consegue carregar"* não se sustenta: slash-commands **podem** ter frontmatter — `owl/research.md` tem. As personas-comando (0/13) simplesmente não têm. Teste por **ausência** viraria desculpa (*"não colocamos lá"*). O teste é **imposição**.
- **Efeito de segunda ordem, e é o melhor pedaço:** isso torna operante a pergunta que o ciclo 7 registrou *"for whoever re-opens that id"* e deixou parada — **`allowed-tools` num comando é imposto como `tools:` num subagent?** Sob o ADR-034 ela deixa de ser curiosidade e vira **pré-requisito de caminho crítico**: sem ela verificada, não se pode alegar assimetria. Pergunta parada há 4 dias entrou no caminho crítico.
- **A verificação decide qual alternativa vale, e isso está escrito:** se `allowed-tools` **for** imposto, a assimetria não existe, a edição vai nas duas metades na forma de cada uma, e a alternativa B do ADR-034 é estritamente melhor que a cláusula. O ADR é **falsificável por construção**.
- **⚠️ Registrado sem maquiagem: isto AFROUXA um guardrail.** O ADR-028 nasceu porque uma edição de uma metade só quase entrou em produção. Mitigação: a assimetria tem que ser **verificada e registrada**, e a metade excluída é **listada**, nunca omitida — o revisor vê a lacuna em vez de não ver nada. Prosa continua exigindo as duas, sem exceção.
- **Onde a cláusula morde de verdade:** a verificação L3 do `evolve.md`. Se ela continuasse exigindo "as duas cópias ou FALHOU" incondicionalmente, o gate reprovaria exatamente as edições que o ADR-034 autoriza. Agora chaveia no que o `arquivo_alvo` **declara** — e exclusão **sem** razão verificada segue **FALHOU**.
- **Os dois candidatos foram atualizados no mesmo commit.** Ambos nomeavam o conflito como bloqueio; virou "resolvido, condicional à verificação". Deixá-los stale faria o ciclo 9 ler um bloqueio que não existe mais — o defeito exato que ocupou o dia inteiro.
- **A classe `Unenforceable prose` do ADR-030 NÃO afrouxou** — registrado no ADR para um leitor futuro não confundir: prosa apresentada como imposta continua sendo o modo de falha.

## [2026-08-12] cycle 9 | `/owl:evolve` autônomo — 3 aceitos, **1 reprovado no gate**, 2 landados
- **Eixo:** `orchestration topology` (round-robin após `inter-agent communication`, ciclo 8). **Cutoff:** 2026-07-13. **Modelo:** `gpt-5.5` (resolvido de `~/.codex/config.toml`; `loop-config.yml` NÃO editado — carve-out). Brief: 3 fontes, 2 ideias, 58 ids decididos injetados. **Fim do streak de 3 ciclos 0-accept.**
- **O ciclo cumpriu as três obrigações que o ciclo 8 deixou escritas** (log de 2026-08-07): pontuou os **dois** candidatos de `ideas/` sem linha no ledger, e levantou a elevação de classe do passo 0.5 do ADR-027. Nenhuma passou batido — que era o modo de falha nomeado.

### A verificação que destravou dois candidatos de uma vez
A sub-questão que o ciclo 7 registrou *"for whoever re-opens that id"* e que o ADR-034 promoveu a **pré-requisito de caminho crítico** foi resolvida na doc primária: **`allowed-tools` num comando NÃO é imposição.** Verbatim: *"It does not restrict which tools are available: every tool remains callable."* É grant de **um turno** que **alarga** — oposto semântico de `tools:`, que é allowlist durável que estreita.
⇒ **Condição do ADR-034 SATISFEITA e verificada** para os dois reabertos. A alternativa B do ADR-034 (*"se for imposto, edite as duas metades"*) foi **testada e falhou** — o ADR era falsificável por construção, foi exercitado e sobreviveu.
⚠️ **Ressalva registrada, não escondida:** existe `disallowed-tools` na metade comando e ela **é** restritiva, mas expira na próxima mensagem. A afirmação verificada é *"sem allowlist durável"*, **não** "sem equivalente nenhum" — dizer o segundo seria overclaim, e o ADR-034 exige razão verificada.
- **Bônus:** os 3 campos de `agent-frontmatter-fields` (`maxTurns`, `memory`, `isolation`) são **todos reais**. O bloqueio caiu por inteiro — e o candidato **mesmo assim não passou** (73), por mérito. Primeira vez que *"reabrir ≠ aceitar"* foi exercido na prática.

### Desfechos (7 ids)
| id | score | status | por quê |
|---|---|---|---|
| `stale-context-block-in-l0-prompt` | **91** | **accepted → ADR-035** | 5 propriedades falsas injetadas no prompt L0 todo ciclo; o prompt **se contradizia internamente** |
| `routing-eligibility-mode` | 84 | ⛔ **rejected no gate L4** | aplicado, reprovado, revertido — ver abaixo |
| `least-privilege-tool-scopes-v2` | **80** | **accepted → ADR-037** | fatia cortada para **1 agente** (chronicler), não 8 |
| `inert-command-frontmatter` | 79 | deferred | cap + adjacência, **não mérito** — cabeça de fila do ciclo 10 |
| `agent-frontmatter-fields-v2` | 73 | deferred | por mérito: sem dado de turnos, teto errado troca custo por ciclo quebrado |
| `pinned-roster-snapshots` | 62 | deferred | fonte única **não verificada** ⇒ ADR-013 barra |
| `loop-machinery-self-audit` | 54 | rejected | elevação de classe do ADR-027 (7 ocorrências) — **duplicata do ADR-033** |

### ⛔ O gate reprovou o ADR-036 — e isso é o item mais valioso do ciclo
`routing-eligibility-mode` (84) foi aplicado a 10 personas-comando e **reprovado pelo @guardian**. O ADR afirmava, depois de checar **um** consumidor (o `/owl:evolve`): *"Nenhum outro consumidor conhecido."* Um `grep` de uma linha refutou:
```
grep -rl 'skill="agents:\|Skill tool: /agents:' .claude/agents/   →  9 de 13
```
`architect.md:83` **instrui**: *"**USE A SKILL TOOL** […] não apenas mencione '@builder' no texto"*. E a doc: *"Use `disable-model-invocation: true` to **block programmatic invocation**"* ⇒ teria quebrado a delegação em 9 agentes, **em silêncio**.
- **A forma do erro, nomeada:** verificar um caso e escrever "nenhum outro" **não é verificação — é a ausência dela com a redação de uma.** Mesma forma que o ADR-015 registrou quando o Δ=0 do `role-ownership` foi generalizado, e que o fitness de 2026-08-07 recusou repetir explicitamente.
- **🔑 Subproduto do gate: P7 é FALSA.** `structural-properties.md` afirma *"hub-and-spoke — especialistas nunca chamam uns aos outros — **VERDADEIRA**"*, com `.claude/commands/agents/*.md` como caminho-prova. **O caminho-prova a contradiz em 9 arquivos.** P7 é classificada como **escolha**, e escolha *"só muda por decisão do dono"* ⇒ **reportado, não consertado.** É a segunda propriedade falsa achada neste ciclo, e esta foi achada **pelo mecanismo funcionando**, não por revisão humana.
- ⛔ **Recusado sob pressão de gate:** reaplicar só nos 4 agentes que ninguém invoca. Redesenhar uma mudança para caber pela brecha que o gate abriu é como mudança ruim entra.

### ADR-035 — o bloco CONTEXT do prompt L0 (91)
O artefato 8a afirmava *"markdown-only, **NO-RUNTIME** library of **~8** agents […] no orchestration engine, no daemon"*. Contra o registro: **P1, P2, P3, P6 falsas** e são **13** agentes. Pior: a correção de 2026-08-07 consertou o bloco irmão (`REJECTED CLASSES`) **do mesmo prompt**, então o prompt montado hoje dizia *"no daemon"* na linha 33 e *"scheduler (launchd)"* na 117.
- **A causa-raiz não é o mecanismo do ADR-033 — é a lista de alvos dele.** O gatilho (a) disparou e funcionou; `structural-properties.md` §"O que depende deste registro" simplesmente **não listava o 8a**. Corrigido nos dois lados: a afirmação **e** o registro.
- **A verificação achou um terceiro arquivo que o plano não previa.** O critério escrito (*"remontar o prompt e confirmar 0 afirmações falsas"*) voltou `markdown-only -> 1`: o mesmo defeito em `research-brief-schema.md:44`. Não teria aparecido por revisão de diff, só por remontar o artefato final. **Ainda aberto de propósito:** 8b entrou por achado, não por regra — *como saber que a lista está completa* segue sem mecanismo.

### ADR-037 — escopo de ferramentas do chronicler (80)
`tools: Read, Write, Edit, Grep, Glob, Bash, Skill, Agent` — **derivada exaustivamente do próprio arquivo**, com tabela ferramenta↔linha-fonte publicada para o revisor conferir em vez de confiar. **Sai:** `WebFetch`/`WebSearch`/MCP. Rationale: a carta dele é *"gerado DO código"* (l.214) e ele lê o repo inteiro — ler-tudo + falar-para-fora é o par a quebrar.
- **@challenger derrubou um overclaim e o ADR foi corrigido:** o texto citava a medição de 1-em-3 vazamentos como se justificasse tirar a rede. **Não justifica** — aquele vazamento é para dentro do artefato. A medição estabelece adjacência a segredo; o argumento do egresso é **estrutural e não medido**. Escrito assim no ADR.
- `Skill` e `Agent` **explicitamente incluídos** — omiti-los reproduziria exatamente o FAIL do ADR-036.
- **Fatia = 1 agente, não 8:** não existe lista de ferramentas declarada em lugar nenhum (`.meta.yaml` não declara `tools`), então cada lista é **julgada**, e lista curta demais degrada em **silêncio**. 7 seguem na fila.

### Falhas e limites deste ciclo, sem maquiagem
- **A ferramenta `advisor` estava indisponível nas 4 tentativas** (overloaded). Nenhuma revisão externa do approach foi obtida. Registrado, não escondido.
- **`pinned-roster-snapshots` foi deferido sem que sua fonte fosse buscada** — o @scout registrou a não-verificação em vez de fingir. Custo: uma ideia julgada com evidência mais fraca do que poderia ter.
- **O resíduo do ADR-037 não foi refutado:** a derivação cobre o que o arquivo **menciona**; comportamento real não documentado ficaria de fora, e falharia em silêncio. Exigiria instanciar o agente — não feito.

### Fechamento do vault (ciclo 9) — feito depois, por revisão
Quatro coisas ficaram para trás na primeira passagem do L5 e foram corrigidas antes do PR ser revisado. Registradas porque **três delas eram a mesma classe de defeito que este ciclo passou o dia consertando** — registro afirmando algo falso sobre o estado do próprio loop.
- **Os dois candidatos pré-carregados ainda diziam `awaiting_scoring: cycle-9` no frontmatter.** Foram pontuados (80 e 73) e um **landou como ADR-037** — mas as páginas não registravam `status`/`score`/`adr`. O L5 manda *"gravar o `adr` de volta no `ideas/<id>.md`"* e o SCHEMA exige esses campos; sem isso o ciclo 10 leria "aguardando pontuação" em ids já decididos. Corrigido.
- **`index.md` não tinha sido atualizado** (passo 5 do SCORE no SCHEMA). Agora tem o bloco do ciclo 9 e a contagem: **112 páginas**, verificada por `ls sources/*.md | wc -l`.
- **Nenhuma página de `sources/` tinha sido criada** (passo 2 do INGEST). Criadas **3** — as três fontes primárias que foram de fato buscadas e citadas verbatim. ⚠️ **A quarta NÃO foi criada de propósito:** `s1` (Managed Agents) não foi buscada, e stub de fonte não lida daria aparência de evidência verificada. É a mesma razão que deferiu `pinned-roster-snapshots`.
- **O ADR-036 usava `Status: REJECTED no gate L4`, fora do enum do `000-template.md`.** Campo que parece schema e não é — a classe `Unenforceable prose` um nível acima. `Rejected` foi **adicionado ao enum** e o ADR-036 realinhado.
- **ADR-037 ganhou uma exclusão explícita:** o `MODO TEAM` (Claude Agent Teams, `chronicler.md:85-89`) é comportamento **documentado** cujas ferramentas o arquivo não nomeia — plausivelmente `SendMessage`/`Task*`, nenhuma na lista. Não cabia na ressalva de *comportamento não documentado*. Declarado fora de escopo com três razões verificáveis (gated por env var experimental, custo 3-5×, não verificável sem instanciar), e virou pré-requisito escrito para o próximo agente do rollout.

## [2026-08-12] human-directed | P7 cindido por escopo (ADR-038) · fila do ciclo 10 resequenciada
Passagem do dono depois do ciclo 9, fora do `/owl:evolve`. Três atos, três linhas `human-directed` (ADR-027). **Nenhum arquivo de agente mudou.**

### O binário que eu levei ao dono estava errado — e a contagem mostra por quê
Perguntei *"hub-and-spoke é a escolha real, ou peer-invocation é o mecanismo real?"*. **Nenhuma das duas.** Verificado nas **duas** metades do par:
```
grep -rl 'skill="agents:|Skill tool: /agents:|USE A SKILL TOOL'
  .claude/agents/           → 9 de 13
  .claude/commands/agents/  → 9 de 13   ← o caminho-prova do P7
```
Mesmos 9 nos dois lados ⇒ **par consistente, sem deriva ADR-028.** E os **4 que não fazem** peer-invocation são `curator`, `scout` (agentes **só do loop**), `team` (o **hub**, ADR-011) e `challenger` (gate que só devolve veredito). **Não é distribuição aleatória — é exatamente o conjunto que vive dentro do loop ou é gate.** Os 9 que fazem são o pipeline **DevFlow**, o mesmo que vai no pack portátil.
Fechando o outro lado, `evolve.md:21`: *"o orquestrador **lê o arquivo** do agente e segue as instruções dele inline"*. O loop **nunca** invoca persona via Skill.

⇒ **O P7 não era falso: era verdadeiro do loop, arquivado como afirmação sobre o sistema inteiro, citando como prova os arquivos exatos que o refutam.** Cindido (ADR-038) em **P7a** (dentro do `/owl:evolve`: VERDADEIRA) e **P7b** (DevFlow interativo: FALSA **por design**). O registro ganha um segundo eixo — **escopo**, ao lado de capacidade×escolha — e toda propriedade nova passa a levar a pergunta *"isto vale em todo lugar, ou num escopo?"*.
- **Alternativa recusada, e é a que mais custava:** remover as instruções de peer-invocation dos 9 agentes para o P7 voltar a ser globalmente verdadeiro. Seriam 18 arquivos mudados para fazer a realidade caber num registro — **consertar o mapa destruindo o território.**

### `routing-eligibility-mode` ENCERRADO, não adiado
A condição registrada era *"depende do dono resolver o P7"*. Cumprida no mesmo dia, com **resposta negativa**: peer-invocation é o mecanismo real do DevFlow, então `disable-model-invocation` nas personas do pipeline quebra exatamente o que o P7b descreve. Não existe versão da mudança que sobreviva naquela superfície. **Nenhuma condição de reabertura permanece aberta** — id cuja condição foi cumprida negativamente e que segue *parecendo* reabrível é o modo de falha que o ADR-033 inteiro combate.
- 📌 A fatia coerente **só nos agentes loop-only** (onde o P7a é o invariante real) está registrada e **não acionada**. Se valer, o ciclo 10 pontua **do zero, com id novo** — ressuscitar este id pela porta que o gate abriu é o movimento recusado sob pressão, e continua recusado sem ela.

### Fila do ciclo 10 resequenciada
- **Cabeça: `mechanism-liveness-verification`** (novo, não pontuado, **sem linha no ledger**). A lacuna: nada verifica se mecanismo que **afirma rodar** de fato roda. O ADR-033 cobre propriedade **expirada**; isto é mecanismo **morto**, e é pior de detectar porque emite **silêncio** em vez de decisão errada. **n=2 do mesmo dia:** o schedule morto ~3 semanas (com `launchctl list` dizendo "carregado, exit 0" enquanto o programa não existia e o log de erro ia pra casca com 0 bytes), e `cost` "TODO" há 2+ ciclos — falta de dado que **deferiu o `agent-frontmatter-fields-v2` (73)**: mecanismo morto custou uma decisão. ⚖️ O FAIL do ADR-036 **não** foi contado como instância — é classe diferente (disciplina de verificação), e contá-la seria fabricar evidência.
- **`inert-command-frontmatter` sai da cabeça** (a página dele dizia que era o primeiro). Mérito e score 79 intactos; o que mudou é o que se sabe da superfície: pôr `description` **não é cosmético** — sem ele a harness cai no primeiro parágrafo, então um explícito **muda quando Claude carrega** aqueles 9 comandos. Mesma classe que acabou de reprovar no gate. Ganhou **pré-condição escrita**: rodar e registrar o grep de consumidores **antes** de qualquer ADR — literalmente o passo que o ADR-036 pulou.
